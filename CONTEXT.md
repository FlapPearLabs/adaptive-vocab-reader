# 词汇阅读领域语言

## 词汇键（wordKey）

词典核心包主词条的小写形式，是个人词汇状态、测试证据、取义和词频分段的唯一身份单位。纯屈折变化的页面词形共享映射目标的词汇键；页面词形本身若是主词条则优先使用自身。
_Avoid_: 状态键、stateKey、规范化单词

## 页面词形（surfaceForm）

用户在网页中实际看到的字面形式，用于保留原文和定位页面位置。它不是独立的状态或证据单位；例如 `went / going / gone` 共享 `go` 的词汇键。
_Avoid_: surface state、surface stateKey

## 词典核心包

由固定 ECDICT 输入和确定性频率规则产生的约 1,000 个主词条本地 dogfood 包；每项只有单词、音标、词性和简短中文释义。扩容词包不属于当前词典核心包。

## 个人词汇状态

用户对某个词汇键当前的 `会`、`不会` 或 `未知` 结论，决定页面提示；由最后一次显式动作（手动标记、首测作答、每日校准作答）写入。普通曝光和查看释义不能改变它。

## 测试证据（AssessmentEvidence）

某个词汇键最近一次首测或每日校准的测试结果，独立于个人词汇状态保存。它是词频区间估计以及“是否已测过”“最久未测”判断的唯一依据；手动标记不产生测试证据。

## 词频区间估计

基于各词频区间内已有测试证据的词计算出的个人掌握量估计，输出「单点估计值＋保守范围」，并标注基于当前词包、不做外推。它只从测试证据派生，不受手动标记影响，也不决定是否隐藏词。
_Avoid_: 词汇量分数、置信区间、CEFR 等级

## 每日校准轮

每天可选完成的一小组测试题，用于补充测试证据、让词频区间估计逐步收敛。它由用户主动发起，可在开始前跳过；它不是背诵、间隔复习、复杂审计或遗忘训练。
_Avoid_: 生词复测、复习计划、背词任务

## 活跃生词表

当前状态为“不会”的单词清单，用于强提示与可选状态核验；它不包含复习阶段、到期时间或学习排程。
_Avoid_: 复习队列、记忆计划

## 强提示

不会词在同页首次出现时的下划线与行内简短中文；同页后续出现仅保留下划线。

## 轻提示

未知词的下划线与悬停查看。用户标记不会后立即升级为强提示。

## 不必要提示

dogfood 人工记录类别之一：用户实际认识某个词，但插件仍然给出提示。

## 释义不可用

dogfood 人工记录类别之一：插件给出的释义明显错误、错配或无法帮助当前阅读。

## 覆盖缺失

dogfood 人工记录类别之一：用户在阅读中实际卡住的词，插件没有给出任何提示。
