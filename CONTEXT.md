# 词汇阅读领域语言

## 词汇键（wordKey）

ECDICT core 主词条的小写形式，同时是取义、词频分段和个人词汇状态的单位。只是屈折变化的页面词形映射到同一词汇键；页面词形本身若是 core 主词条，则优先使用自身。
_Avoid_: 状态键、取义主词条、规范化单词

## 页面词形（surfaceForm）

用户在网页中实际看到的字面形式，用于保留原文和定位 DOM。它不是独立的个人词汇状态单位；例如 `went / going / gone` 共享 `go` 的词汇键。
_Avoid_: surface state、小写 surface-form stateKey

## 词典核心包

由固定 ECDICT 输入和确定性频率规则产生的约 1,000 个主词条本地 dogfood 包；每项只有单词、音标、词性和简短中文释义。约 10,000 词扩容不属于当前词典核心包。

## 个人词汇状态

用户对某个词汇键的 `会`、`不会` 或 `未知` 结论。初测、简单更新测试和用户明确动作可以改变它；普通曝光和查看释义不能改变它。

## 词频区间画像

由十个词频区间的初测与简单每日更新形成的个人词汇量估计，输出「单点估计值＋保守范围」，并标注基于当前词包、不做外推。它从频段内所有 `wordKey` 的聚合状态实时派生（单词状态为单一真相源），不自动决定是否隐藏未知词。

## 强提示

不会词在同页首次出现时的下划线与行内简短中文；同页后续出现仅保留下划线。

## 轻提示

未知词的下划线与悬停查看。用户标记不会后立即升级为强提示。

## 每日校准轮

每天可选完成的少量短测试（默认 5 题，按十频段比例从当前词包抽样），用于简单更新词频区间画像；它不是背诵、间隔复习、复杂审计或遗忘训练。每轮答案会改写对应 `wordKey` 的持久状态，并触发词频区间画像实时重算；跳过则无变化。
_Avoid_: 生词复测、复习计划、背词任务

## 活跃生词表

当前状态为“不会”的单词清单，用于强提示与可选状态核验；它不包含复习阶段、到期时间或学习排程。
_Avoid_: 复习队列、记忆计划
