# 词汇阅读领域语言

## 规范化单词

ECDICT 主词条的小写形式。网页词形可以映射到它以命中词典，但不因此继承会／不会状态。

## 词典核心包

由固定 ECDICT 输入和确定性频率规则产生的约一万主词条本地包；每项只有单词、音标、词性和简短中文释义。

## 个人词汇状态

用户对一个规范化单词的 `会`、`不会` 或 `未知` 结论。初测、活跃生词状态核验和用户明确动作可以改变它；普通曝光和查看释义不能改变它。

## 词频区间画像

由随机初测、校准题与隐藏词审计题形成的、带不确定度的个人掌握预测。它只为未确认词估计 `P(会)`，不能改写单词级状态；网页主动标记不进入该画像。

## 高置信不提示

未知词满足掌握预测、后验下界和随机隐藏词审计三项门槛时的静默展示决策。它是可被后续审计推翻的预测，不是“会”状态。

## 单次答对审计标记

初测四选一答对而进入会状态的内部待核验标记。该词会立即停止提示，但仍可被随机审计；审计通过或失败后标记即清除。

## 隐藏词审计

从未提示的单次答对词与高置信预测会词中按频段随机抽取的测试。它用于测量“不会却未提示”的漏提示率，并在结果出现后更新状态与清理审计标记。

## 强提示

不会词在同页首次出现时的下划线与行内简短中文；同页后续出现仅保留下划线。

## 轻提示

未达到高置信不提示门槛的未知词的下划线与悬停查看。用户标记不会后立即升级为强提示。

## 每日校准轮

每天可选完成的 5–30 道短测试，用于估计词汇范围、核验活跃生词状态并审计提示边界；它不是背诵、间隔复习或遗忘训练。
_Avoid_: 生词复测、复习计划、背词任务

## 活跃生词表

当前状态为“不会”的单词清单，用于强提示与可选状态核验；它不包含复习阶段、到期时间或学习排程。
_Avoid_: 复习队列、记忆计划

## 受控回填

每日校准轮中某个候选桶不足时，空缺只在未知边界校准和隐藏词审计之间按确定性交替规则补足；两者也没有候选时才少出题。活跃生词状态核验不因回填增加题数。
_Avoid_: 复习回填、强行凑题
