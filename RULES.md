# 浏览器词汇学习插件：现行产品规则

更新时间：2026-07-22

本文件是当前唯一的产品规则来源。用户最新明确指示优先；研究笔记只提供事实或建议，不自动成为需求。

## 产品目标

- **[已确认]** 做一个供单用户本地 dogfood 的 Chrome 英文网页词汇阅读插件，不是全文翻译器、词典编纂平台或模型产品。
- **[已确认]** 正常 HTTPS 英文网页正文是当前范围；暂不支持 PDF、字幕、OCR、电子书、跨域 iframe 和浏览器内置页。
- **[已确认]** 项目尚未实现扩展；当前不得假称已有可运行产物。

## 词典

- **[已确认]** 默认内置 ECDICT 的固定输入快照，机械筛选约 10,000 个高频单词级主词条。
- **[已确认]** 运行时只保留单词、音标、词性和简短中文释义；`frq` 正序主排，缺失时用 `bnc` 正序补位，`tag` 不参与默认排序；`exchange` 仅从实际屈折代码生成词形到主词条的命中映射。
- **[已确认]** 缺音标、中文释义、频率排名或明显异常的候选直接淘汰并递补。词性优先取 ECDICT `pos` 列；该列为空时，只可从 `translation` 每行开头的显式词性前缀机械提取，仍没有词性则淘汰。不得使用 LLM 或人工逐条清洗补全。
- **[已确认]** 输入版本、输入哈希、筛选规则、最终条数和淘汰原因必须可重复记录。
- **[待确认]** ECDICT 释义的逐条公开再分发权利链；在此之前只用于个人 dogfood，不公开发布。

## 词汇测试、状态与提示

- **[已确认]** 状态单位是规范化单词：`会`、`不会`、`未知`。词形映射只帮助查词，不传播状态；不实现义项、MWE、俚语、派生关系或词族传播。
- **[已确认]** 首次固定完成 50 道“英文单词→四选一中文释义＋不确定”题，十个词频区间各五题。答对为会；答错或不确定为不会；未测试词保持未知。
- **[已确认]** 初测答对立即为会并撤除提示；同时保留“单次测试答对、待随机审计”的内部标记。审计答对后清除标记；答错或不确定立即改为不会并加入生词表。
- **[已确认]** 随机初测、校准题与隐藏词审计题构成未确认词的掌握预测画像；预测绝不能改写某一单词的会／不会状态。网页中用户主动标记只更新该词与其活跃生词表，不作为全局画像训练样本。
- **[已确认]** 用户“会／不会”立即覆盖旧结果；普通曝光、查看释义和浏览次数不改变状态。
- **[已确认]** 用户每天可选进行 `N` 道“每日校准轮”，其中 `5 ≤ N ≤ 30`。目标是测量用户的词汇范围与提示边界，不是背单词、间隔复习或遗忘训练。题目可核验活跃生词表中的单词，也可从未知边界词与当前未提示词中抽取；测试不得阻塞阅读。
- **[已确认]** 活跃生词表是“当前确认不会”的显示与状态核验清单，不是学习计划或按时间到期的复习队列。词只有在用户明确标会或其后续状态核验答对时才移出；答错或不确定则维持不会。
- **[已确认]** 每日校准轮按 `活跃生词状态核验 / 未知边界校准 / 隐藏词审计 = floor(N / 5) / ceil(2N / 5) / (N - floor(N / 5) - ceil(2N / 5))` 分配。故 `N=5` 为 `1/2/2`，`N=30` 为 `6/12/12`。活跃状态核验只更新被测词，不进入全局画像；边界校准与隐藏词审计更新画像。
- **[已确认]** 未知词在边界校准中无论答对、答错或不确定，都只更新画像、仍保持未知；不创建会／不会状态或单次答对审计标记。单词状态只由初测、活跃状态核验、隐藏词审计或用户明确动作改变。
- **[已确认]** 某一校准桶候选不足时，空缺只在未知边界校准与隐藏词审计之间按安装种子和测试轮次确定的交替顺序回填；活跃生词状态核验不获得额外名额。两类候选也都耗尽时才少出题。旧的 `3/1/1`、`6/2/2` 和“复测→审计→校准”回填规则已撤销。
- **[已确认]** 会＝不提示；不会＝同页首次下划线加行内简短中文、后续仅下划线可悬停查看；未知默认轻提示，唯有满足已确认高置信门槛的“预测会”未知词可以不提示。
- **[已确认]** 高置信不提示必须同时满足：该词仍为未知；当前画像的 `P(会)` 后验均值 `≥ 0.85`；`P(会)` 的单侧 90% 后验下界 `≥ 0.70`；并且当前画像版本已积累至少 20 道随机隐藏词审计，且该审计集的单侧 90% Wilson 漏提示率上界 `≤ 30%`。任一条件不满足即保留轻提示。
- **[已确认]** PAV 合并 `m` 个词频段后，合并块后验固定为 `Beta(m + ΣC, m + ΣU)`，保留每个原始频段的 `Beta(1,1)` 先验，并使块后验均值与权重 `C + U + 2` 的 PAV 均值一致。

## 明确不做与冻结项

- **[已确认]** 不实现 Kaikki、OEWN、COW、多源义项对齐、Reasonix/DeepSeek 清洗、Ollama、LM Studio、云模型、自动或手动上下文解释、模型缓存或全文翻译。
- **[已确认]** 已删除相关旧规格、实验、提示词、清洗计划和模型原型；仅保留 `outputs/kaikki-tooltip-cleaner/` Skill 作为独立技能资产，不作为当前产品流程。

## 交付、安全与下一步

- **[已确认]** 先做约 1,000 词的可丢弃垂直切片，真实跑通“正文扫描→本地快查→三档提示→会／不会→刷新后保留”，再以同一方案扩到约 10,000 词。
- **[已确认]** 未获用户明确授权，不得实现生产功能、commit、push、创建 Issue/PR、部署、发布、下载词典或修改远端配置。
- **[已确认]** 用户已于 2026-07-22 授权下载固定 ECDICT 快照并运行确定性构建脚本；已生成仅供本机 dogfood 的 1,000 词核心包。该授权不包含扩展实现、公开发布或扩容至约 10,000 词。
- **[已确认]** 已完成掌握预测与主动校准的首轮原型；每日校准轮配比、边界校准状态迁移、PAV 合并后验与候选不足时的受控回填均已确认并完成原型验证。任何规格确认都不自动获得 commit、远端写入或发布授权。
- **[已确认]** “词汇展示与测试策略模块”是 V0.1 的最高测试 seam：所有展示决策、冻结题目计划与状态变更只能由它计算；页面、弹窗、存储与后台协调器只能消费其结果，不得重算画像、阈值、抽样或审计清理。
- **[已确认]** 用户已授权将 V0.1 施工 tickets 发布到 GitHub。已发布 #1–#5，均使用 `ready-for-agent` 标签和 GitHub 原生 blocker 关系；当前唯一无 blocker 的实施 ticket 是 #1。
