# 浏览器词汇学习插件：现行产品规则

更新时间：2026-07-30

本文件是当前唯一的产品规则来源。用户最新明确指示优先；研究笔记只提供事实或建议，不自动成为需求。

## 施工规格与验收优先级

- **[已确认]** 2026-07-30 起重新对齐 V0.1。`docs/specs/` 中 2026-07-22 的两份施工规格及现有 GitHub tickets 仅作历史施工证据，在新一轮 `/grill-with-docs` 与 `/to-spec` 完成前，不再作为新增实现的授权。
- **[已确认]** GitHub Issue 是施工索引、依赖关系和局部验收清单，不能替代 `RULES.md` 与已批准 Spec。冲突时按“用户最新指示 → `RULES.md` → 已批准 Spec → Issue”处理。
- **[已确认]** 执行代理的自述、测试数量和局部“全绿”都不构成产品验收。验收必须映射到当前已批准的用户闭环、规则和真实交付路径。
- **[已确认]** V0.1 最高层验收 seam 为真浏览器 E2E（升级现有 `e2e-verify.cjs` 覆盖「首测结果展示 + 每日更新 + 持久化」完整闭环）；真人 dogfood 门槛（见交付与下一步）为叠加的人工验收门，不替代 E2E。

## 产品目标

- **[已确认]** 做一个供单用户本地 dogfood 的 Chrome 英文网页词汇阅读插件，不是全文翻译器、词典编纂平台或模型产品。
- **[已确认]** 正常 HTTPS 英文网页正文是当前范围；暂不支持 PDF、字幕、OCR、电子书、跨域 iframe 和浏览器内置页。
- **[已确认]** 当前已有可构建、可用真实 Chrome 验证的 1,000 词本地垂直切片；但词汇量估计、简单每日更新和真人 dogfood 闭环尚未完成，因此 V0.1 尚未产品验收。

## 词典

- **[已确认]** 当前 dogfood 只内置 ECDICT 固定输入快照产生的约 1,000 个高频单词级主词条。扩展到约 10,000 词必须等 1,000 词真人 dogfood 完成后再决定。
- **[已确认]** 运行时只保留单词、音标、词性和简短中文释义；`frq` 正序主排，缺失时用 `bnc` 正序补位，`tag` 不参与默认排序；`exchange` 仅从实际屈折代码生成词形到主词条的命中映射。
- **[已确认]** 缺音标、中文释义、频率排名或明显异常的候选直接淘汰并递补。词性优先取 ECDICT `pos` 列；该列为空时，只可从 `translation` 每行开头的显式词性前缀机械提取，仍没有词性则淘汰。不得使用 LLM 或人工逐条清洗补全。
- **[已确认]** 输入版本、输入哈希、筛选规则、最终条数和淘汰原因必须可重复记录。
- **[待确认]** ECDICT 释义的逐条公开再分发权利链；在此之前只用于个人 dogfood，不公开发布。

## 词汇测试、状态与提示

- **[已确认]** 个人词汇状态是词典主词条的 `会`、`不会`、`未知`。页面词形若只是屈折变化，则与映射到的主词条共享状态，例如 `went / going / gone → go`。
- **[已确认]** 词典主词条优先：页面词形本身若是合法 core 主词条，就使用自身的状态与释义；因此 `could` 与 `can` 独立。不做派生词、词族、义项、MWE 或俚语的状态传播。
- **[已确认]** 首次固定完成 50 道“英文单词→四选一中文释义＋不确定”题，十个词频区间各五题。答对为会；答错或不确定为不会；未测试词保持未知。
- **[已确认]** 初测答对立即为会并撤除提示；答错或不确定立即为不会并加入生词表。V0.1 不要求为单次答对额外构建冻结计划、审计标记或防篡改协议。
- **[已确认]** 用户“会／不会”立即覆盖旧结果；普通曝光、查看释义和浏览次数不改变状态。
- **[已确认]** 活跃生词表是“当前确认不会”的显示与状态核验清单，不是学习计划或按时间到期的复习队列。词只有在用户明确标会或其后续状态核验答对时才移出；答错或不确定则维持不会。
- **[已确认]** V0.1 词汇量估计只以十个词频区间的答题结果给出，且必须同时展示「单点估计值」与「保守范围」（例如「约掌握 ~620/1000 核心词」+「约 540–700」），并明确标注「基于当前 1,000 词覆盖估计，不做外推」。
- **[已确认]** 估计含义只针对当前 1,000 词包内的掌握量，不外推到更大词汇规模、不输出 CEFR 等外推量级；估算算法按十频段聚合、词包大小作为参数，未来扩容时可复用（但 V0.1 不实现外推）。
- **[已确认]** 预测（自动/模型推断）不得改写单词级状态，也不用于自动隐藏未知词。
- **[已确认]** V0.1 每日更新：默认 5 题/天；题目从当前 1,000 词包内按十频段比例抽样（首测 50 题的迷你版），不引入新词；用户可随时跳过，跳过当天不更新、无惩罚、不阻塞阅读。
- **[已确认]** 每日答案改写该 `wordKey` 自身持久状态（答对→升为会/确认会；答错→降为不会），十频段估计实时从频段内所有 `wordKey` 的聚合状态重算；跳过＝无变化。`wordKey` 状态为单一真相源，估计永远从其派生。
- **[已确认]** 会＝不提示；不会＝同页首次下划线加行内简短中文、后续仅下划线可悬停查看；未知＝轻提示。
- **[已确认]** 高置信自动隐藏、隐藏词审计、Pool B、Wilson 区间、PAV/Beta 后验、复杂三桶配额与受控回填均推迟；它们不是当前 V0.1 验收条件。

## 拒绝局部过度设计

- **[已确认]** 不仅拒绝整体范围膨胀，也拒绝局部过度设计：任何子系统的完整度、抽象层级、协议强度和测试数量，不得超过当前用户闭环所需。
- **[已确认]** 优先顺序由用户价值决定，而不是由某个内部模块“还能做得更完整”决定。不得在真实词汇量估计、简单更新与 dogfood 完成前，继续扩建审计协议、哈希完整性、状态版本、通用 facade 或新测试 seam。
- **[已确认]** 每个实现 ticket 必须交付一条可演示的窄垂直切片，并回答“用户现在多得到了什么”。只增加内部泛化、防御性协议或 mock 测试，不构成独立交付。
- **[已确认]** 新抽象、迁移、审计层或统计模型必须由当前 Spec 的具体验收场景驱动；仅为可能的未来需求不得提前实现。
- **[已确认]** 现有超出简化 V0.1 的代码不立即大规模重写或删除；先在新 Spec 中标记“保留但不扩建 / 简化 / 删除”，再以最小变更处理。

## 明确不做与冻结项

- **[已确认]** 不实现 Kaikki、OEWN、COW、多源义项对齐、Reasonix/DeepSeek 清洗、Ollama、LM Studio、云模型、自动或手动上下文解释、模型缓存或全文翻译。
- **[已确认]** 已删除相关旧规格、实验、提示词、清洗计划和模型原型；仅保留 `outputs/kaikki-tooltip-cleaner/` Skill 作为独立技能资产，不作为当前产品流程。

## 交付、安全与下一步

- **[已确认]** 先用约 1,000 词完成真实可用闭环：正文阅读与手动状态、50 题首测、简单十频段词汇量估计、简单每日更新、刷新/重启持久化和真人 dogfood。只有该闭环验收后，才决定是否扩到约 10,000 词或重开复杂审计。
- **[已确认]** 真人 dogfood 完成标准（扩容与 V0.1 验收门槛）：连续使用 ≥ 7 天且每天实际打开插件阅读；累计阅读真实英文网页 ≥ 20 篇；误提示率可接受（靠本地日志判定，无系统性误标）；词典缺失可容忍且不阻断阅读；刷新/重开浏览器后 `wordKey` 状态与每日进度无丢失；词汇量估计用户本人可读懂且觉得有用（口头确认即可）。上述数字为建议值，可由用户调整。全满足＝dogfood 完成，可进入约 10,000 词评估与 V0.1 验收。
- **[已确认]** 未获用户明确授权，不得实现生产功能、commit、push、创建 Issue/PR、部署、发布、下载词典或修改远端配置。
- **[已确认]** 用户已于 2026-07-22 授权下载固定 ECDICT 快照并运行确定性构建脚本；已生成仅供本机 dogfood 的 1,000 词核心包。该授权不包含扩展实现、公开发布或扩容至约 10,000 词。
- **[已确认]** 旧掌握预测原型只保留为历史证据，其复杂校准、后验与回填方案不继续扩建。
- **[已确认]** 复杂审计 seam（`strategy/audit.ts`、`worker/auditValidation.ts`、`shared/auditPlanVersion.ts` 及 `AuditMarker`/`AuditPlan`/状态版本）与已发布 #1–#5 只保留为历史证据，标记为「保留但冻结」：不删、不扩建、不被任何 V0.1 用户路径调用。2026-07-30 close-out 增加的 FNV-1a 完整性哈希与 worker fake-strategy seam 测试同样冻结，不得继续加固。
- **[已确认]** 新一轮顺序为 `/grill-with-docs` 明确未决项 → `/to-spec` 产生本地规格并经用户批准 → `/to-tickets` 拆分垂直切片并经用户批准。未获新的远程写入授权前，不发布或修改 GitHub Issues。
