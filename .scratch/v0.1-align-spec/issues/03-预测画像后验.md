# 03 — 预测画像后验（Beta/PAV + Wilson）

**What to build:** 在策略模块内实现十频段 Beta/PAV 后验，输出每个 unknown 词的 `P(会)` 后验均值、单侧 90% 下界与上界；纯函数、可单测，作为高置信不提示与审计门槛的输入（本票暂不改变 unknown 仍返回 light 的行为，仅产出可消费的画像）。

**Blocked by:** 01 — 统一词汇策略 seam

**Status:** ready-for-agent

- [ ] strategy 模块导出 `profileWord(word, evidence)` 返回 `{mean, lower, upper}`（Wilson 单侧）
- [ ] 单测覆盖后验均值与 Wilson 边界计算
- [ ] 十频段证据来源自画像状态与校准轮（依赖 02 的 bandEvidence 落盘）
