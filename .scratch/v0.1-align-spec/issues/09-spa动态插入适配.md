# 09 — SPA 动态插入适配

**What to build:** 对 SPA / 无限滚动页面的动态插入正文做增量标注，复用现有增量更新路径，不退化成全页重扫；覆盖路由切换与节点动态插入场景。

**Blocked by:** 05 — Wilson 漏提示率判定与高置信不提示；06 — 每日校准轮；08 — 审计作答消费

**Status:** ready-for-agent

- [ ] DOM 变更/路由监听 + 动态正文增量标注
- [ ] SPA fixture（动态插入/无限滚动）E2E 验证标注生效且不重扫全页
