# 04 — 隐藏词审计桶抽取

**What to build:** 建立隐藏词审计候选池（对齐 Spec B §8）：候选池为「单次初测答对词（仍为会、来源 initial-correct、待审计标记）」与「高置信不提示的未知词」两类；隐藏词审计在两类候选之间交替抽取，某类为空时由另一类补位，保证可用于随机审计的题量 ≥20 道；审计桶由策略模块产出，供高置信判定与审计消费循环共用。

> 注：本 ticket 早先误写为「高置信判定为不会 / 边界不确定」，与权威规格 Spec B §8 冲突。已校正为「单次初测答对词 + 高置信不提示未知词」。代码（`strategy/audit.ts`）原本就按规格实现，本票仅修正文案。

**Blocked by:** 03 — 预测画像后验；02 — 持久化快照补全

**Status:** deferred（V0.1 高置信机制未建，池 B 恒空；本票属 #3 全量范围，本轮不实施）

- [ ] 审计桶选取两类候选（单次初测答对词 / 高置信不提示未知词）并按规则交替
- [ ] 审计桶题量 ≥20 时返回可用；不足时按 Spec B 受控回填规则处理
- [ ] 单测覆盖交替抽取与回填边界
