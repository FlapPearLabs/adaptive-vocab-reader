# 02 — 持久化快照补全

**What to build:** 持久化快照从「仅含 words/auditMarkers/initialTest」扩展为含测试轮次、十频段证据与审计统计，使画像、校准与审计的可解释行为能被刷新后保留与审计；含从旧快照的向前迁移。

**Blocked by:** 01 — 统一词汇策略 seam

**Status:** ready-for-agent

- [ ] `VocabSnapshot` 增 `rounds`/`bandEvidence`/`auditStats` 字段（对齐 Spec A §5）
- [ ] storage 读写适配新字段，旧快照缺字段时给默认值（迁移不丢数据）
- [ ] E2E 断言刷新后三类新字段保留
