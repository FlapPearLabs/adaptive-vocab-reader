# V0.1 Dogfood Interaction / Personalization / Fallback / Assessment Realignment 规格（待审）

## 1. STATUS

- **状态**：**DRAFT / 待 DOCUMENT 复审与用户批准**。
- **来源**：由已通过网页版 GPT DOCUMENT 复审 PASS 的 Grill 决议（D1–D17，`work/2026-08-11-v0.1-dogfood-realignment-grill-decisions.md`，审查基线 fd90d4e）正式转换而来。**用户已于 2026-08-12 明确批准进入 `/to-spec`**。
- **与旧 Spec 的关系**：本 Spec 是 **V0.1 Dogfood Product Realignment 的唯一施工规格**。它**取代** `2026-08-06-V0.1-查询交互提示与测评词包解耦规格.md`（APPROVED）中与本轮 D1–D17 冲突的合同（旧 Spec 文件保留为历史，不回写、不修改）；旧 Spec 中未被取代的合同由本 Spec 继承（继承清单见 §2 与 §20）。
- **本 Spec 不授权**：任何生产代码修改、测试修改、ticket 拆分、commit/push（除本审查分支本身）、merge、部署、provider 选择或 10,000 测评包扩容。
- **后续步骤**：DOCUMENT 复审 PASS + 用户最终批准 → `/to-tickets` 拆生产垂直切片 → Codex handoff → 用户「开始开发」。
- **初稿日期**：2026-08-12。

## 2. BACKGROUND_AND_DOGFOOD_EVIDENCE

### 2.1 真实 dogfood 证据（用户首次真实使用，Hacker News 等）

用户确认以下路径**基本正常**（作为 REGRESSION_INVARIANTS，本轮不得破坏）：

1. 点击「不会」后：WordState 立即变为 learning；
2. 页面立即出现红色强提示；
3. learning 词正常进入生词本；
4. 生词本内能够进行掌握状态操作；
5. 当前 query dictionary 大部分查询路径正常。

同时暴露以下 6 个产品问题（本轮全部解决，不以只修 P0 结束）：

| # | 优先级 | 问题 | dogfood 表现 |
|---|---|---|---|
| 1 | P0 | **主机页原生交互冲突** | 透明 span + click 拦截（`preventDefault` + 弹菜单）与网页争夺 click/pointer/selection；HN 等链接页无法自然点开链接；button/checkbox/input/拖选/Cmd/Ctrl/Shift-click 存在同类风险 |
| 2 | P1 | **个性化提示失效** | 简单词（如 `woman` 一类）仍被系统提示，而用户已在测评中通过更难的词——系统表现为「完全不认识我」；hint 是全局频率启发式而非个性化 |
| 3 | P1 | **本地词典 miss 是死路** | miss 词只能看到「当前词典未收录」：无翻译、不能反馈会/不会、不能进生词本——明显半成品 |
| 4 | P1 | **测评/校准/重测 UX 不透明** | 产品设计者本人都不理解：初测 50 题、每日五题、calibration、estimate、manual 反馈的影响、重测清什么、入口在哪 |
| 5 | P2 | **全词可查显冗余** | 「每一个单词都能查」感觉冗余（影响不大）；modifier 门控后需验证是否自然解决 |
| 6 | P2 | **hint 密度/视觉质量** | 「稀疏」≠「合理」：误提示、密度、红/灰视觉层级、不同页面类型是否吵、是否破坏阅读节奏 |

### 2.2 关键事实（调查核实，非推测）

- 查询词典当前已是**全量 ECDICT**：121,340 query-eligible / 40,090 有效频率 / 81,250 缺频率；T₀ = S[⌊n/2⌋] = 22662。
- 测评包保持**固定 1,000 词冻结口径**（本会话无任何用户对 10,000 扩容的明确确认，本轮不涉及扩容）。
- 当前实现代码级事实（`extension/src/content/annotator.ts` 委托 click handler 对 `.avr-word` 执行 `preventDefault` + 弹会/不会菜单；`pointerover` 无修饰键门控恒出 tooltip；`strategy/hint.ts` 候选输入仅频率 + 显式状态过滤；lookup miss 仅「当前词典未收录」tooltip；`INITIAL_TEST_RESET` 保留 Evidence 与 WordState）。

### 2.3 继承的旧合同（未被取代，继续有效）

核心产品等式（可查询≠必须灰线、无灰线≠不可操作、未测试≠潜在不会、known≠不可查询、查询词典≠测评包）；查询词典/测评包职责分离（E/F 已生效，限定本地范围）；Evidence≠WordState 双真相源；估计只读 Evidence（单点 + Wilson 保守范围，manual 不进估计）；首测 50 题 / 每日五题规则；schema 3 与既有迁移；审计冻结；拖选辅助入口（未收录拖选维持静默）；tooltip 几何；query eligibility 独立于 hint eligibility（缺频率词仍可查询）；local ECDICT 再分发 fail-closed；隐私边界（不上传正文/历史、无遥测）。

## 3. PRODUCT_GOAL

让插件成为**可以长期挂在浏览器里使用的阅读工具**，三个最高层判断：

- **A（PAGE INTERACTION）**：插件永久启用时，用户在绝大多数时间可以忘记它的存在，直到自己主动按住 Ctrl 查词。
- **B（COVERAGE/FALLBACK）**：本地词典未收录一个普通英文词时，用户仍能完成「理解 → 判断会/不会 → 生词管理」闭环，不变成死路。
- **C（PERSONALIZATION）**：用户已通过足够测评证明掌握较简单词汇后，系统不再机械地给大量明显简单词画灰线。

同时保持：PRODUCT UX + DATA SEMANTICS + PRIVACY + ENGINEERING CLEANLINESS 同时成立，不牺牲任何一个来假装完成。

## 4. PAGE_NATIVE_INTERACTION_FIRST

**正文 click 所有权（网页）**：正文中任何 click（含链接导航）永远归网页/浏览器。插件**绝不** preventDefault / stopPropagation 正文交互；不因单词包装阻止网页 click；不抢占 button/checkbox/input/textarea/select/form submit/context menu/drag/text selection/Cmd-click/Ctrl-click/Shift-click。

**浏览器所有权**：浏览器自身定义的修饰键组合语义（Cmd-click 新标签、Ctrl-click 右键/新标签、Shift-click 选区等）保持原生，插件不得覆盖。

**Extension 所有权**：仅插件创建的 UI（tooltip、其内按钮、生词本/弹窗等）由插件管理；这些 UI 上的事件仅作用于插件自身。

**验收**：真实 Puppeteer keyboard/mouse 矩阵（§21），非仅 dispatchEvent。

## 5. MODIFIER_GATED_LOOKUP_STATE_MACHINE

### 5.1 状态

| 状态 | 含义 |
|---|---|
| IDLE | 无 Ctrl；页面零插件 UI；所有网页交互原生 |
| CTRL_ARMED | Ctrl 已按下；等待 hover 命中可查询词 |
| WORD_TOOLTIP | Ctrl 按住且指针悬停在 queryable word 上；显示查询 tooltip |
| TOOLTIP_INTERACTION_SESSION | 指针已进入 extension-owned tooltip；keyup 豁免生效；可普通点击会/不会 |
| CLOSED | UI 已关闭（tooltip 隐藏），回到 IDLE 或 CTRL_ARMED |

### 5.2 事件转移（D1/D3/D6/D7 闭合）

| 事件 | 转移 |
|---|---|
| Control keydown | IDLE/CLOSED → CTRL_ARMED |
| pointer enter word（CTRL_ARMED 且 word queryable） | → WORD_TOOLTIP，显示 tooltip |
| pointer leave word（仍按住 Ctrl，未进入 tooltip） | WORD_TOOLTIP → CTRL_ARMED，隐藏 tooltip |
| Control keyup（指针不在 tooltip 内） | WORD_TOOLTIP/CTRL_ARMED → IDLE，隐藏 tooltip |
| pointer enter tooltip | WORD_TOOLTIP → TOOLTIP_INTERACTION_SESSION（豁免激活） |
| Control keyup（指针在 tooltip 内） | 保持在 TOOLTIP_INTERACTION_SESSION（**D3 有界例外，D7**） |
| pointer leave tooltip | TOOLTIP_INTERACTION_SESSION → CLOSED，隐藏 tooltip |
| button click（tooltip 内 会/不会） | 写入状态（仅 WordState），tooltip 关闭 |
| Escape | 任意状态 → CLOSED |
| scroll / navigation / DOM removal（目标词消失） | 任意状态 → CLOSED |
| 无 Ctrl 的任何 hover | 不产生查询 UI（IDLE 保持） |

约束：不得产生永久 sticky tooltip；tooltip 会话只由「指针在 tooltip 内」维持，不依赖计时器；tooltip 显示不拦截正文事件。

### 5.3 link-query 共存（D6）

链接文字内单词在 Ctrl+hover 下可查询；点击（含 Cmd/Ctrl/Shift/右键）保持链接原生行为。tooltip 与链接共存时，链接 click 不被 tooltip 或包装 span 拦截。

## 6. TOOLTIP_AND_FEEDBACK_INTERACTION

- tooltip 内容：页面词形（surfaceForm）、音标、词性、简短中文释义（query dictionary canonical entry；miss 词显示「当前词典未收录」）+ 「会 / 不会」按钮。
- **feedback 只发生在 extension-owned UI（tooltip 按钮）**；正文 click 永不用于 feedback（D6/D7）。
- 交互会话见 §5.2；按钮点击写入 `WordState`（known/learning，source=manual），**不写 AssessmentEvidence、不改估计**（继承规则）。
- tooltip 几何沿用既有规则（优先上方、必要时下方、不超视口、不侵入 sticky/header、滚动后定位正确）。
- 未收录词（lookup miss）的 tooltip：显示「当前词典未收录」+ 会/不会按钮（D10 允许反馈）。

## 7. PERSONALIZED_HINT_POLICY

展示决策拆为两层（D8/D9/D16）：

1. **显式状态提示**（继承）：known → 不提示；learning → 红色强提示（同页首次含行内中文）。
2. **候选提示（light/灰线）**：
   - **global bootstrap**：证据不足时的回退阈值（沿用既有 effectiveFrequencyRank 口径，T₀ 公式保留为参数 seam）；
   - **个人频段画像（主）**：由每频段近期证据窗口（§9）派生「能力下界」——窗口全 known → 下压该频段及更简单频段的未测词提示；窗口出现 learning → 恢复（双向）；
   - **已测单词级特例（辅）**：已测词按自身最新 AssessmentEvidence 判定（known → 该词不提示；learning → 该词提示），优先于频段级；
   - **manual override**：用户显式 known/learning 状态永远优先（状态过滤保留，候选只作用于无显式反馈词）；
   - **recovery**：任何 learning 证据（含 daily 新答案）立即恢复该频段/该词的提示，不依赖重置。

约束：候选不落盘、不改写 WordState；不恢复概率画像/PAV/Beta/高置信自动隐藏；hint-ineligible（无有效频率）词不参与频段级候选但保持可查询。

## 8. ASSESSMENT_EVIDENCE_BOUNDARY

- **Evidence MAY INFORM hint policy**（§7 的频段画像与单词特例输入），**BUT MUST NOT CREATE / REWRITE WordState**（D8）。
- 估计仍只读 AssessmentEvidence（单点 + Wilson 保守范围，继承规则）；manual 反馈不进入估计分子/分母（继承）。
- 包外词 / 未收录临时键不写 Evidence、不进估计（继承 + D10）。
- 内容脚本不得把 Evidence 写入用户状态；候选生成前后 WordState snapshot 不变（负断言）。

## 9. RECENT_K_EVIDENCE（D16 正式算法合同）

每频段（band）：

1. 从 AssessmentEvidence map 过滤出该 band 内的记录（以测评包 band 映射为准；无 band 的键不参与）；
2. 按 `assessedAt` **降序**排序；
3. 取**最近 K 个不同 wordKey** 的「当前最新 Evidence」（K 参数，保守 bootstrap = 5，dogfood 校准）；
4. 同一词重测：覆盖该词 Evidence（仅最新一条），并将该词移到窗口最近位置；**不保存以前的作答（attempt）**；
5. 窗口内全 known → 下压；出现 learning → 恢复（§7）。

**实现约束**：窗口是现有 Evidence map 的**派生视图**，不新增持久化字段/表/历史；不升 schema；幂等可复现（同一输入同一输出）。

## 10. UNRESOLVED_TOKEN_IDENTITY_AND_MANUAL_STATE（D10）

- **词典可解析性 ≠ 用户反馈资格**：本地查询词典 miss 的普通英文 token 允许：反馈会/不会、进入生词本。
- **临时身份**：状态键 = 规范化 surface token（trim、去首尾标点、小写、去空白）；是字符串键，**不升级 schema**（与 F 一致）。
- **manual 状态资格**：miss 词可写 `WordState`（known/learning，source=manual）。
- **Evidence 隔离**：miss 词不写 AssessmentEvidence（无 band，估计天然跳过）。
- **生词本未收录元数据**：popup 生词本中该类条目如实标注「未收录」（展示层 re-resolve：当前词典仍无法解析则显示标注；解析后按 §11 升级）。
- **拖选辅助入口**：维持既有规则（拖选未收录 → 无法唯一解析 → 静默不弹）；未收录词的 notebook 主入口为 tooltip 会/不会（D10 主交互路径），拖选辅助入口不扩大。
- **历史无法解析键**：保守保留，不静默删除（继承）。

## 11. CANONICAL_RECONCILIATION（D17）

- **惰性迁移触发**：当持有 `WordState` 的 surface key 被查询词典解析到 canonical lemma（词典更新后 lookup 命中，如 `womans`→`woman`）时，一次性迁移。
- **迁移动作**：把该 surface key 的 `WordState` 移到 canonical wordKey；删除 surface key；幂等（重复触发无副作用）。
- **碰撞仲裁**（canonical 已有 `WordState` 时，复用 schema 3 既有规则，非新发明）：`updatedAt` 较新者胜 → 相同则 manual 优先 → 仍相同则 learning 优先（避免漏提示）。
- **未解析保留**：仍无法解析的历史 surface key 保守保留。
- **迁移边界**：只迁 `WordState`；**不创建 AssessmentEvidence、不升 schema**。
- **跨标签同步**：迁移后 canonical 词状态经既有 STATE_UPDATED 广播同步。
- **生词本去重**：迁移后每词一键，天然去重；「未收录」标注在解析后自动升级为完整元数据展示。
- **触发位置**（实现 seam）：内容脚本 lookup 命中路径 / popup 生词本遍历路径均可触发，均为读路径幂等操作。

## 12. LOOKUP_FALLBACK（D11）

- **local-first**：本地查询词典命中 → 立即本地查询。
- **local miss → remote（用户主动、单词级、按需）**：
  - 只允许在**用户主动发起查询**（Ctrl+hover 显示 tooltip 且词 miss）时触发；
  - 只发送**当前 word/token**；
  - 不发送网页正文、URL、页面标题、整句或浏览历史（默认禁止）。
- **降级**：离线或失败 → 明确提示「未收录，且当前无法获取网络释义」；不静默失败。
- **Provider 未选择**：本 Spec 不选择 provider、不预设 API、不写死域名（§13/§25，implementation-before-STOP）。

## 13. NETWORK_PRIVACY_BOUNDARY

- 禁止上传：页面正文、整句、URL、标题、浏览历史（任何 provider 下都禁止，负断言）。
- 只允许传输：单个 miss word/token（用户主动触发时）。
- provider、域名权限、API 许可、缓存再分发许可、大陆网络可用性在 **research + 用户确认前**：不建生产网络实现、不加入泛化 `host_permissions`（fail-closed）。
- 不新增遥测/日志/dashboard（继承）。

## 14. CACHE_AND_PERSISTENCE

- 抽象合同（不冻结具体格式/存储）：若 provider 许可允许，可本地缓存 miss 词的远程释义（单词语义级）；若 provider 许可不允许缓存，不得缓存。
- **provider research 前不冻结 cache format / 缓存表 / schema**；缓存内容不得包含上下文。
- 缓存许可判定为 implementation-before-STOP 项（§25/§26）。

## 15. NOTEBOOK_BEHAVIOR

- learning 词进入生词本；known/「已掌握」移除（继承）。
- 展示所有 `status=learning` 且可展示元数据的 identity（含测评包外词，继承）+ 未收录临时键条目（标注「未收录」，D10）。
- canonical reconciliation 后（§11）：条目去重、标注自动升级。
- 生词本只读 WordState；不读 AssessmentEvidence（继承）。
- 历史无法映射 key 保守保留、不展示、不删除（继承）。

## 16. ASSESSMENT_CALIBRATION_RETEST_UX（D12/D13）

- **首测**：50 题、十频段各五题（继承）；完成前不出现每日入口（继承）。
- **每日校准**：每日五题、奇偶频段轮换（继承）；答案滚动进 §9 窗口自动更新边界（派生，无需新决策）。
- **重新完整测评 = 半重置（D12）**：清空 AssessmentEvidence、dailyTest、completedRoundIndex（估计从新证据重新积累；新证据不足时 estimate=unavailable）；**保留全部 WordState**（生词本、manual learning/known 不丢）；重测作答仍按现行「后写覆盖」写入同词状态；入口带确认对话框与解释文案。
- **popup「我的词汇水平」总览（D13）**：首测状态 / 点估计 / 保守范围 / 今日校准进度（0/5）/ 最近校准 / [开始今日校准] / [重新完整测评]（带确认）+ 一行解释「手动标记的会/不会只影响阅读提示和生词本，不会伪装成正式测试成绩」；「了解这些数字」可展开说明（默认收起，按需解释估计/校准/重测语义）。

## 17. EVERY_WORD_QUERYABILITY（D14）

- 保留广覆盖查询词典；所有 queryable 词默认可 Ctrl+hover 查询（invisible capability：无 Ctrl 时零视觉/零交互噪声）。
- **不重新绑定 queryability 与 hint eligibility**；不因冗余感收缩查询面；不提前优化。

## 18. HINT_DENSITY_AND_VISUAL_POLICY（D15）

- 验收 = 结构化测量 + 人工 dogfood，**不冻结未经验证的固定 SLA**：
  - 每百词灰线密度（perf seam 输出参考值）；
  - learning 强提示数；
  - 人工三数字（不必要提示 / 释义不可用 / 覆盖缺失，继承 dogfood 记录规则）；
  - 按页面类型（HN / 长文 / 文档 / 论坛）分别记录。
- 数据积累后由用户判断是否调整算法参数（K、边界）；不先调颜色掩盖算法问题。

## 19. SCHEMA_AND_MIGRATION

- **目标：schema 3 不变**。
- D16 = Evidence map 的派生视图（§9），无新增持久化。
- D17 = 惰性状态迁移（§11），无 schema 变更。
- **若实现证明 schema bump 必需**：STOP，返回用户重新决策；不得提前升 schema（STOP_CONDITION: SCHEMA_CHANGE_REQUIRED）。
- 既有 schema 3 迁移与仲裁规则不变（继承）；快照隐私约束不变（无 URL/正文/句子/历史）。

## 20. COMPATIBILITY

- main 当前用户 profile（既有 WordState/AssessmentEvidence）升级后行为不变（除被 D1–D17 取代的合同外）。
- 既有 known/learning 状态不丢失；生词本数据不被静默清空（继承）。
- 旧 unresolved/临时键：按 §10/§11 处理（保守保留 + 惰性迁移）。
- 跨标签同步、reload/restart 持久化（继承）。
- 同源 iframe 边界、开放 Shadow DOM、SPA/characterData 增量、CSS 隔离、tooltip 几何、选区路径（继承）。

## 21. REAL_CHROME_ACCEPTANCE（P0 最高 gate）

**必须使用真实 Puppeteer keyboard/mouse 路径，不得仅 dispatchEvent**。覆盖：

- **ANCHOR**：普通 click / Cmd-click / Ctrl-click / Shift-click / right-click —— 保持浏览器原生行为（浏览器自身定义的特殊组合不得覆盖）。
- **BUTTON**：真实 mouse click。
- **FORM**：input focus/typing、textarea focus/typing、checkbox、radio、select、submit。
- **TEXT**：drag selection、copy、context menu。
- **SCROLL**：wheel/scroll。
- **PLUGIN**：Ctrl keydown → hover queryable word → tooltip 出现；Ctrl keyup → tooltip 消失；无 Ctrl hover → 无查询 UI。
- **LINK QUERY（D6）**：链接文字 Ctrl+hover 查询可用 + 链接原生 click 保持完好。
- **TOOLTIP 会话（D7）**：pointer 移入 tooltip → keyup 豁免 → 松开 Ctrl 点击按钮写状态 → 移出关闭。
- **未收录（D10）**：miss 词 tooltip 显示「未收录」+ 会/不会可写状态；不写 Evidence。
- **迁移（D17）**：预置 surface learning → 词典更新解析 → 惰性迁移到 canonical、surface 删除、红提示/生词本连续。
- **重测（D12）**：半重置后 Evidence 清空、WordState 保留、估计 unavailable→重新 available。
- **hint（D9/D16）**：窗口滚动（全 known 下压 → learning 恢复 → 再全 known 重新下压）；WordState 零改写。

## 22. NEGATIVE_ASSERTIONS

- 正文文本交互无 preventDefault / stopPropagation（P0 矩阵负断言）。
- Evidence 不创建/改写 WordState（候选生成前后 snapshot 不变）。
- manual 反馈不进估计（估计前后数值不变）。
- 无上下文/URL/标题/历史上传（网络请求内容断言）。
- 无测试 attempt 历史（D16 派生视图）。
- 无 schema bump（schemaVersion 不变）。
- provider 未确认前无泛化远程权限/网络实现（manifest 断言）。
- queryability 与 hint eligibility 不重新耦合（缺频率词仍可查询）。
- 未收录词不写 AssessmentEvidence；拖选未收录零写入。

## 23. CURRENT_IMPLEMENTATION_GAPS

基于当前 main 代码（`extension/src/content/annotator.ts`、`pageScanner.ts`、`strategy/hint.ts`、`popup.ts`、`worker/index.ts` 等）的真实差距：

| OLD（当前行为） | TARGET（本 Spec） | OWNER |
|---|---|---|
| hover 无条件出 tooltip；click 命中 `.avr-word` 即 preventDefault + 弹菜单 | Ctrl 门控；正文 click 归网页；tooltip 会话反馈 | 交互层（§4/§5/§6） |
| 候选输入仅频率 + 显式状态，不含 Evidence | Evidence 参与（频段画像 + 单词特例，D8/D16） | hint 层（§7/§9） |
| lookup miss 仅「未收录」提示、无反馈无生词本 | miss 可反馈会/不会、可进生词本（临时键 + 未收录标注，D10） | 查询/notebook 层（§10/§15） |
| 无 canonical reconciliation | surface key 惰性迁移至 canonical（D17） | 状态层（§11） |
| 无网络回退（local-first 严格） | 用户主动单词级按需回退（provider 未定 fail-closed，D11） | 网络层（§12/§13/§14） |
| 「重新测评」无明确语义（保留 Evidence） | 半重置（D12） | worker/popup（§16） |
| popup 无「词汇水平」总览 | 总览页 + 可展开说明（D13） | popup（§16） |
| 已实现项（回归不拆票）：learning 红提示/生词本、known 移出、跨标签同步、Evidence 隔离、tooltip 几何、拖选、schema 3 | 保持不变 | — |

## 24. REQUIREMENTS

稳定 Requirement ID，全部可追溯至 D1–D17 或继承 RULES（无 Spec-only requirement）：

### R-INT（交互）
- R-INT-1：无 Ctrl 时零插件 UI、原生操作优先（D1/D4）
- R-INT-2：Ctrl+hover 显示查询 tooltip；无 Ctrl 不显示（D1）
- R-INT-3：禁止 Ctrl+click 查词（D2）
- R-INT-4：keyup(Ctrl) 关闭 UI，指针在 tooltip 内时豁免（D3/D7）
- R-INT-5：正文 click 归网页，绝不 preventDefault/stopPropagation，不破坏 Cmd/Ctrl/Shift/右键（D4/D6）
- R-INT-6：链接/interactive 内文本可 Ctrl+hover 查询，与原生 click 共存（D6）
- R-INT-7：tooltip 为 extension-owned 交互 UI；feedback 只发生在 tooltip（D7）
- R-INT-8：状态机（IDLE/CTRL_ARMED/WORD_TOOLTIP/TOOLTIP_INTERACTION_SESSION/CLOSED）与事件转移（§5.2）

### R-HINT（提示）
- R-HINT-1：Evidence 可影响 hint policy 但不创建 WordState（D8）
- R-HINT-2：频段级近期画像（主）+ 已测单词特例（辅）（D8）
- R-HINT-3：双向边界（全 known 下压 / learning 恢复）（D9）
- R-HINT-4：K 窗口 = 最近 K 个不同 wordKey 最新 Evidence（assessedAt 降序），无历史、不升 schema（D16）
- R-HINT-5：K 参数化，bootstrap=5，dogfood 可调（D9/D16）
- R-HINT-6：manual 状态过滤优先；候选不落盘（继承）

### R-EVD（Evidence 边界）
- R-EVD-1：估计只读 Evidence；manual 不进估计（继承）
- R-EVD-2：包外/未收录键不写 Evidence、不进估计（继承 + D10）

### R-UNR（未收录）
- R-UNR-1：miss 词可反馈会/不会、可进生词本（D10）
- R-UNR-2：临时键 = 规范化 surface token；不升 schema（D10）
- R-UNR-3：生词本未收录条目标注「未收录」；展示层 re-resolve（D10）
- R-UNR-4：surface key 惰性迁移至 canonical；仲裁复用 schema 3；未解析保留；幂等（D17）
- R-UNR-5：拖选辅助入口对未收录维持静默（继承）

### R-NET（回退/网络）
- R-NET-1：local-first；miss 后用户主动、单词级、按需回退（D11）
- R-NET-2：只发送单词本身；禁止上下文/URL/标题/历史（D11）
- R-NET-3：失败/离线明确降级（D11）
- R-NET-4：provider/权限/缓存许可未确认前 fail-closed（D11）
- R-NET-5：缓存仅当 provider 许可允许；格式不提前冻结（D11/§14）

### R-ASSESS（测评/重测）
- R-ASSESS-1：重测 = 半重置（清 Evidence/daily/轮次，留 WordState）（D12）
- R-ASSESS-2：重测后 estimate 证据不足时 unavailable（D12）

### R-UX（测评 UX）
- R-UX-1：popup「我的词汇水平」总览（首测/估计/校准进度/重测入口）（D13）
- R-UX-2：可展开说明 + 一行解释（manual ≠ 正式成绩）（D13）

### R-QUERY（查询面）
- R-QUERY-1：保留广覆盖查询词典；invisible capability（D14）
- R-QUERY-2：queryability 与 hint eligibility 不重新绑定（D14）

### R-QUAL（hint 质量）
- R-QUAL-1：结构化测量 + 人工 dogfood，不设固定 SLA（D15）

### R-COMPAT / R-PRIVACY / R-REG（兼容/隐私/回归，继承）
- R-COMPAT-1：schema 3 不变；D16 派生视图、D17 惰性迁移（§19）
- R-COMPAT-2：既有状态/生词本不丢失（继承）
- R-PRIVACY-1：不上传正文/历史；无遥测（继承）
- R-PRIVACY-2：网络只传单词（D11）
- R-REG-1：learning→红→生词本、known 移出、跨标签同步、Evidence 隔离等零回归（D5）

## 25. OPEN_DECISIONS

- **PROVIDER_SELECTION = DEFERRED_WITH_IMPLEMENTATION_STOP**：本 Spec 不选择 fallback provider；research（API/许可/成本/大陆网络可用性/rate limit/缓存再分发许可）后由用户确认；确认前不建生产网络实现、不加泛化 `host_permissions`。**不因该 deferred 项阻塞本 Spec 的 DOCUMENT review。**
- **K = bootstrap 5，dogfood-tunable**（参数 seam，非 SLA）。
- **hint density = measurement only**（无固定阈值）。
- 无其他阻塞性 OPEN；核心 identity/persistence policy 已由 D1–D17 闭合。

## 26. STOP_CONDITIONS

- **PROVIDER_DECISION_REQUIRED**：进入网络实现前 provider 未确认 → STOP。
- **NETWORK_PERMISSION_DECISION_REQUIRED**：加入远程权限前未获用户批准 → STOP。
- **CACHE_LICENSE_DECISION_REQUIRED**：缓存格式/再分发未获 provider 许可确认 → STOP。
- **SCHEMA_CHANGE_REQUIRED**：实现证明 schema bump 必需 → STOP 返回用户。
- **PRIVACY_BOUNDARY_CONFLICT**：任何要求上传上下文/URL/历史的方案 → STOP。
- **MAIN_BASELINE_MOVED**：交付前 origin/main 规则出现真实冲突 → STOP 报告。
- **RULES_SPEC_CONFLICT**：实现与 RULES/本 Spec 冲突且未获用户确认 → STOP。
- **P0_NATIVE_INTERACTION_UNRESOLVED**：P0 矩阵（§21）未全部通过前 → 不合并/不交付。

---

## DECISION → SPEC REQUIREMENT COVERAGE（双向闭环）

| D | 覆盖 Requirement |
|---|---|
| D1 | R-INT-1/2 |
| D2 | R-INT-3 |
| D3 | R-INT-4 |
| D4 | R-INT-1/5 |
| D5 | R-REG-1（+§20） |
| D6 | R-INT-5/6 |
| D7 | R-INT-4/7/8 |
| D8 | R-HINT-1/2 |
| D9 | R-HINT-3/5 |
| D10 | R-UNR-1/2/3 |
| D11 | R-NET-1/2/3/4/5、R-PRIVACY-2 |
| D12 | R-ASSESS-1/2 |
| D13 | R-UX-1/2 |
| D14 | R-QUERY-1/2 |
| D15 | R-QUAL-1 |
| D16 | R-HINT-4/5 |
| D17 | R-UNR-4 |

反向：所有 R-* 均可追溯至 D1–D17 或继承 RULES（§2.3）；无 Spec-only requirement。

## CROSS-CONTRACT VALIDATION

| 维度对 | 结论 |
|---|---|
| interaction × browser | 通过：R-INT-5/6 保证原生 click 与修饰键语义；§21 P0 矩阵验收 |
| interaction × feedback | 通过：feedback 仅在 extension-owned tooltip（R-INT-7）；正文 click 永不用于 feedback |
| feedback × state | 通过：按钮写 WordState(manual)、不写 Evidence（R-HINT-6、R-EVD-1） |
| state × notebook | 通过：learning 进、known 移出；未收录标注 + D17 去重（R-UNR-3/4） |
| Evidence × hint | 通过：Evidence inform hint 但不创建 WordState（R-HINT-1） |
| Evidence × estimate | 通过：估计只读 Evidence；manual 不进（R-EVD-1） |
| retest × Evidence | 通过：半重置清 Evidence → 估计重新积累（R-ASSESS-1/2） |
| retest × WordState | 通过：半重置保留 WordState（R-ASSESS-1） |
| unresolved × canonical reconciliation | 通过：D10 临时键 + D17 惰性迁移 + schema 3 仲裁（R-UNR-2/4） |
| fallback × privacy | 通过：只传单词、禁止上下文（R-NET-2、R-PRIVACY-2） |
| fallback × permission | 通过：provider 未确认前 fail-closed（R-NET-4） |
| cache × license | 通过：仅 provider 许可允许时缓存（R-NET-5、STOP_CONDITION） |
| schema × lazy reconciliation | 通过：D16 派生视图、D17 惰性迁移、schema 3 不变（R-COMPAT-1） |
| queryability × hint eligibility | 通过：不重新绑定（R-QUERY-2） |
