# V0.1 UX 集成规格（基于外部 UX_SPEC_V1.2.1 的仓库权威版）

## 1. 状态与权限边界

- **状态**：本地集成规格，**PHASE-SPEC-INT（2026-09-10）产出**。本文件是 `docs/specs/` 下的当前施工规格之一，**不直接授权实现、commit、push、合并、发布或 Issue/PR 操作**。
- **唯一产品规则来源**：[`RULES.md`](../../RULES.md)（`[已确认]` 条目为本规格的权威输入）。**`RULES.md`、已批准的产品规格、`CONTEXT.md` 与权威领域/存储契约，在身份、持久化、词典 schema 与测评逻辑问题上绝对优先于外部 UX 文档。**
- **领域语言**：[`CONTEXT.md`](../../CONTEXT.md)。
- **架构决定**：[ADR-0004 词汇键与测试证据分离](../adr/0004-词汇键与测试证据分离.md)；ADR-0003 已被其部分取代。
- **上游输入（外部冻结文档，非仓库权威）**：`docs/specs/2026-09-04-UX_SPEC_V1.2.1.md`（见 §2）。
- **当前实现基线**：[`docs/CURRENT_IMPLEMENTATION_BASELINE.md`](../CURRENT_IMPLEMENTATION_BASELINE.md)（VERIFIED，2026-09-09）。
- **本阶段审计报告**：[`work/governance/2026-09-10-ux-spec-integration/REPORT.md`](../../work/governance/2026-09-10-ux-spec-integration/REPORT.md)。
- **决策状态（2026-09-10 `PHASE-DEC-TICKETS`）**：`DEC-1`~`DEC-5` **全部 CLOSED**（见 §10），落点为 `RULES.md`「V0.1 呈现层与流程裁决」。
- **实施 ticket 资格**：**`READY_FOR_TICKET_DECOMPOSITION`**（见 §12）；本批 ticket 见 [`work/tickets/2026-09-10-v0.1-ux-delta/`](../../work/tickets/2026-09-10-v0.1-ux-delta/README.md)。
- **延后候选**：「本页 / This Page」（DEC-4）与「Settings」（DEC-2）**不在 V0.1**，见 §14。

**本文件不做的三件事**：

1. 不新增未经 `RULES.md` 授权的产品行为；
2. 不为外部 UX 文档反向改写仓库领域/身份/持久化契约；
3. 不在此阶段拆分实施 ticket（见 §12）。

---

## 2. 输入、基线与远端真相

| 项 | 值 |
|---|---|
| 外部冻结 UX 文档 | `docs/specs/2026-09-04-UX_SPEC_V1.2.1.md` |
| 该文档状态字串 | `DESIGN FROZEN — AUTHORITY RECONCILED — READY FOR REPOSITORY INTEGRATION REVIEW` |
| 该文档引入 commit | `58a86f77b0358a96c04e7e07baa9900f33186813`（`origin/main`，docs-only；前置 `e4f947b`） |
| 该文档内容 SHA-256（274 行 / 22,673 B） | `d6570918be68189c133ed1fc8756ddd3645aee968a01758f7a81db84548bbcc4` |
| 本阶段治理起点 HEAD | `d5fa1440993f5564c45d5297100f853b74230c5e`（`governance/restart-baseline-2026-09-04`） |
| 本阶段观测到的 live `origin/main` | `58a86f77b0358a96c04e7e07baa9900f33186813` |
| 权威快照修复（`PHASE-DEC-TICKETS` §1） | `f22dd62730844cd63009d73bb1ea1dbf09f50904` —— 以 `--no-ff` 正常合并 `origin/main` 把冻结 UX 文档世系引入集成分支；不 rebase、不 force-push、不复制或改写冻结正文 |
| 冻结 UX 文档修复后校验 | 路径 `docs/specs/2026-09-04-UX_SPEC_V1.2.1.md` 可于本分支 HEAD 读取；blob `34c950272537c36a932aea01309b8b7c46cb2959` 与 `origin/main` 完全一致；SHA-256 `d6570918…bbcc4` 未变 |
| 后续观测到的 live `origin/main`（`PHASE-DEC-TICKETS` 开工时） | `58a86f77b0358a96c04e7e07baa9900f33186813`（未变） |
| 已验证实现基线 | `main@333c3628c5adabd1d69f96b9043eb7a109825eb0` 的 fresh 复现（RESUME-01，2026-09-09） |

> **注意**：`58a86f7` 相对 RESUME-01 基线 `333c362` 只增加了两个 docs-only commit（新增并格式化该冻结 UX 文档），**未改动任何生产代码**。因此 RESUME-01 的实现事实对本规格仍然有效。

---

## 3. 三态标注约定（强制）

本规格所有规范性条目必须落入且仅落入下列三种状态之一。混用或省略视为规格缺陷。

| 标记 | 含义 |
|---|---|
| **`CURRENT_IMPLEMENTATION_FACT`** | 2026-09-09 fresh 验证（或本阶段源码/E2E 复核）**已确认存在于 main** 的行为。任何人不得把它当作待实现工作。 |
| **`NEW_TARGET_BEHAVIOR`** | 外部 UX V1.2.1 引入、仓库当前**不存在**或与当前行为**实质不同**、且不与仓库权威冲突的目标行为。这是唯一允许进入实施 backlog 的类别。 |
| **`HISTORICAL_SUPERSEDED`** | 历史上被提出但已被 `RULES.md` / 本规格 / 外部 UX V1.2.1 自身纠正取代的行为。仅作存史，**不得重新实现**。 |

以下缩写在矩阵中使用：`FACT` / `NEW` / `SUPERSEDED`。

---

## 4. 保留的仓库契约（外部 UX 不得覆盖）

以下契约由仓库权威定义，外部 UX 文档已显式声明服从（`UX_SPEC_V1.2.1` §1.2）。

| 契约 | 位置 | 保留要点 |
|---|---|---|
| `wordKey` | `CONTEXT.md` / ADR-0004 / `RULES.md`「词汇键」 | ECDICT 主词条小写形式；core 主词条优先；屈折词形映射共享同一键。**不由 UX 引入词元/词族理论。** |
| `DictEntry` | `extension/src/shared/types.ts:19` | 字段名固定为 `phonetic` / `pos` / `translation` / `effectiveFrequencyRank?`。**禁止引入 `ipa`、`zh`、`detailZh`、`nuance` 等别名或旧字段。** |
| `WordState` | `types.ts:40` | `status` + `source` + `updatedAt`；只由最后一次显式动作写入。 |
| `AssessmentEvidence` | `RULES.md`「词汇状态与测试证据」 | 估计 / 是否已测 / 最久未测的唯一读取源；manual 标记不写入。 |
| `QuizQuestion` | `types.ts:79` | `word` / `band` / `options[4]` / `correctOptionIndex` / `unsureIndex`（恒为 4，独立于四个中文选项）。 |
| `QuizAnswer` | `types.ts:70` | 判别联合：`{kind:'option', optionIndex}` \| `{kind:'unsure'}`。 |
| 隐私边界 | `RULES.md`「存储与迁移」/ 08-06 规格 §11 | 快照不得含 URL、域名、页面标题、正文、句子、浏览历史。 |
| 估计语义 | `RULES.md`「词汇量估计」 | 单点 + 双侧 90% Wilson 保守范围，钳制 0–词包大小；不输出 CEFR 等外推量级。 |

**外部文档自身的服从声明**（`UX_SPEC_V1.2.1` §1.2.2 / §1.2.3）：不指定 schema 迁移、不发明新持久化模型、不把 `ipa`/`zh` 定义为生产字段名。本规格采纳该声明并提升为仓库约束。

---

## 5. 四向对账矩阵

对账对象：① 冻结 `UX_SPEC_V1.2.1`；② RESUME-01 VERIFIED 实现基线；③ 仓库权威（`RULES.md` / 已批准规格 / ADR）；④ 本阶段的源码与 E2E 复核。

分类取值（每条唯一）：`ALREADY_IMPLEMENTED_AND_VERIFIED` / `ALREADY_IMPLEMENTED_BUT_UX_DIFFERS` / `NEW_REQUIRED_CHANGE` / `DOC_ONLY_REALIGNMENT` / `SUPERSEDED_OLD_REQUIREMENT` / `CONFLICT_REQUIRES_PRODUCT_DECISION` / `OUT_OF_SCOPE` / `UNKNOWN`。

### 5.1 权威边界与领域契约

| ID | UX 条款 | 分类 | 依据 |
|---|---|---|---|
| U-1 | §1.1 交互/展示态与权威领域持久化二分 | `DOC_ONLY_REALIGNMENT` | 架构上已二分（`content/` 只读展示；`worker/storage.ts` 持久化）；外部文档将其显式化的表述需并入本规格而非改代码 |
| U-2 | §1.2.1 `wordKey` 用仓库既有语义 | `ALREADY_IMPLEMENTED_AND_VERIFIED` | ADR-0004 + core 优先规则已实现并被 E2E `abilities→ability` 验证 |
| U-3 | §1.2.2 复用既有持久化契约、不发明新 schema | `ALREADY_IMPLEMENTED_AND_VERIFIED` | 无新增存储模型；包外词沿用同字符串键（OPEN_DECISIONS F） |
| U-4 | §1.2.3 `DictEntry` 字段名 `phonetic/pos/translation/effectiveFrequencyRank`，禁 `ipa`/`zh` | `ALREADY_IMPLEMENTED_AND_VERIFIED` | `types.ts:19-28` 命中；全库无 `ipa`/`zh`/`detailZh`/`nuance` 字段 |
| U-5 | §1.2.4 生词本＝活跃学习词，不是全量被动词倾倒 | `ALREADY_IMPLEMENTED_AND_VERIFIED` | `popupNotebook.ts` 过滤 `status==='learning'` 且查询词典可解析 |
| U-6 | §1.2.5 隐私：网页文本/句子/URL/标题/域名/浏览历史一律不持久化、不传输 | `ALREADY_IMPLEMENTED_AND_VERIFIED` | `RULES.md`「存储与迁移」+ E2E 快照最小性断言（`e2e-verify.cjs:459`） |

### 5.2 阅读面状态矩阵（UX §2）

| ID | UX 条款 | 分类 | 依据 / 差异 |
|---|---|---|---|
| U-7 | Default/Known 完全继承宿主排版、无提示、无 hover 高亮 | `ALREADY_IMPLEMENTED_BUT_UX_DIFFERS` | 行为已达成（known 只剩 `avr-word` 空类，E2E `e2e-verify.cjs:472`）；但 CSS 只显式声明 `color:inherit`，缺 `font-family/size/weight/line-height/letter-spacing` 的 `inherit !important` → 宿主 `span{}` 选择器仍可污染（见 D-4） |
| U-8 | Unknown/Hinted 悬停显示轻量 tooltip，悬停不改状态，不自动弹完整浮层 | `ALREADY_IMPLEMENTED_AND_VERIFIED` | `annotator.ts:268` `pointerover` → `showTooltip`（surfaceForm/phonetic/pos/translation 四行）；无写状态路径；完整操作层需 click |
| U-9 | Unknown/Hinted 点击打开 **Word Inspection Popover** | `NEW_REQUIRED_CHANGE` | 当前 click 打开的是极简操作菜单（`.avr-action-menu`，仅「会 / 不会」两枚按钮，无词头/音标/词性/释义展示） → D-2 |
| U-10 | Learning 首现：实线下划线 + 行内中文释义 | `ALREADY_IMPLEMENTED_AND_VERIFIED` | `avr-strong-first::after` + `showInlineTranslation = isLearning && occurrenceCount===1` |
| U-11 | Learning 重复出现：仅下划线、不重复行内释义 | `ALREADY_IMPLEMENTED_AND_VERIFIED` | `pageScanner.ts:133` 与 `annotator.ts:426`（`index===0 && showInlineTranslation`） |
| U-12 | 行内释义格式 `{posPrefix}{translation}`（如 `adj. 短暂的`）、11–12px 斜体衬线、`#78350f`/60%、`line-height:1`、`user-select:none`、0.35em 间距 | `ALREADY_IMPLEMENTED_BUT_UX_DIFFERS` | 当前为 `【translation】`（`annotator.ts:373`）、`#c0392b`、`0.85em`、`vertical-align:super`、**无词性前缀**、**无 `user-select:none`** → D-5 |
| U-13 | 提示下划线配色：light 琥珀点线、learning 琥珀实线 | `ALREADY_IMPLEMENTED_BUT_UX_DIFFERS`（**DEC-1 已裁定 `AMBER_SYSTEM`**） | 下划线机制与点线/实线强度区分**已实现**（`text-decoration`；E2E `layoutShiftScore 0`）；仅**色相**不同：当前 light `#7f8c8d` 点线、strong `#e74c3c` 实线。`RULES.md` 原「红色强提示」规范已按 DEC-1 重标为琥珀系（保留原文存史），08-06 规格 7 处「红色」条款已加 DEC-1 重标注 → delta **D-6** |
| U-14 | 选中普通词：浮出胶囊按钮（居中于选区上方）、`mousedown` 抢占、`removeAllRanges()`、写入 learning | `ALREADY_IMPLEMENTED_BUT_UX_DIFFERS` | 机制齐备（`pageScanner.ts:143-189`）；差异：文案「加入生词本」、位置在选区**左下**而非居中上方 → D-7。**DEC-3=`CHINESE_FIRST`**：UX 原文 `Mark as Learning · 不会` 的**中英混排被否决**，V0.1 采用简洁中文（图标可选） |
| U-15 | 选区校验：多词/含空白/连字符/标点/纯数字一律拒绝，不做拼接 | `ALREADY_IMPLEMENTED_AND_VERIFIED` | `normalizedSelectedWord()`：trim 首尾标点、含 `\s` 或纯数字返回 null |
| U-16 | `QUERY_DICTIONARY_NOT_CONTAINED`：视觉等同正文、零持久化、拖选恢复静默、显式查询可回 `当前词典未收录` | `ALREADY_IMPLEMENTED_AND_VERIFIED` | `data-unresolved="true"` span 无样式类；E2E AC-10（`e2e-verify.cjs:617-628`）验证 hover 显示该文案、click 不弹菜单、快照读写前后深度相等 |

### 5.3 元数据解析失败（UX §2.1）

| ID | UX 条款 | 分类 | 依据 |
|---|---|---|---|
| U-17 | `METADATA_RESOLUTION_FAILURE` 不是独立词汇状态；不得改写 `WordState` / `DisplayDecision`；known+失败仍为纯文本、light+失败仍点线、learning+失败仍实线但省略行内释义 | `ALREADY_IMPLEMENTED_AND_VERIFIED` | 当前代码**不存在**任何按元数据可用性改写状态/展示决策的分支：`pageScanner.ts:118-135` 的 decision 只由 `WordState` 与频率阈值决定，元数据仅作透传；释义缺失时行内释义自动省略（`annotator.ts:372` 仅在有 translation 时挂 `data-translation`）。不变式**结构性成立** |
| U-18 | 检查浮层/轻提示内显示兜底文案 `释义暂不可用`，禁止合成占位释义 | `NEW_REQUIRED_CHANGE` | 全库检索无 `释义暂不可用`；当前元数据缺失时 hover **静默不显示任何 tooltip**（`annotator.ts:279` 直接 return）→ D-1 |

### 5.4 排版与隔离（UX §3 / §8）

| ID | UX 条款 | 分类 | 依据 |
|---|---|---|---|
| U-19 | §3.2 下划线呈现：点线（light）/ 实线（learning），实现手段自由但必须零布局位移 | `ALREADY_IMPLEMENTED_AND_VERIFIED` | `text-decoration` 实现；E2E `layoutShiftScore 0`；配色差异另计于 U-13 |
| U-20 | §3.4 浮层排版（词头/音标/词性/释义/句内上下文/按钮）参考值 | `NEW_REQUIRED_CHANGE` | 依赖 D-2（浮层本身不存在） |
| U-21 | §8.3 CSS 隔离：Shadow DOM **或**严格前缀 BEM | `ALREADY_IMPLEMENTED_AND_VERIFIED` | 全部注入类名使用 `avr-` 前缀；UX 为「或」关系，满足其一即可 |
| U-22 | §8.2 DOM 安全：保留原文、顺序、空白、标点、选择行为 | `ALREADY_IMPLEMENTED_AND_VERIFIED` | `annotateTextNode` 按精确 index 切分并保留原文大小写；E2E 覆盖 |

### 5.5 交互契约（UX §4）

| ID | UX 条款 | 分类 | 依据 |
|---|---|---|---|
| U-23 | §4.1 悬停只出轻提示、不弹完整浮层、不改状态 | `ALREADY_IMPLEMENTED_AND_VERIFIED` | 见 U-8 |
| U-24 | §4.2.2 浮层几何：上方优先、不足时下翻、左右视口 12px 安全边距、不遮挡目标词、滚动同步 | `NEW_REQUIRED_CHANGE`（**须复用既有定位 seam**） | tooltip 侧已实现并 fresh 验证（上方优先/下翻/不侵入 sticky/不越视口/滚动后正确）；但**操作菜单当前硬编码 `left=rect.left, top=rect.bottom+4`，无翻转、无夹取、无滚动处理** → D-2 必须复用 `positionTooltip` 所依赖的 `calculateTooltipPosition` seam，禁止另写一套 |
| U-25 | §4.2.3 点击「会」/「不会」后立即提交并自动关闭 | `ALREADY_IMPLEMENTED_AND_VERIFIED` | `annotator.ts:239-244`：触发 `actionHandler` 后 `hideAnnotationActionMenu()` |
| U-26 | §4.2.4 Esc 关闭（外部点击 / X 关闭均不改状态） | `NEW_REQUIRED_CHANGE`（部分） | 外部点击关闭已实现（`annotator.ts:248-250`）；**无 Esc 处理**、无 X 图标 → D-3 |
| U-27 | §4.3 拖选恢复路径的完整执行语义 | `ALREADY_IMPLEMENTED_BUT_UX_DIFFERS` | 语义齐备，差异见 U-14 → D-7 |

### 5.6 测评与估计（UX §5）

| ID | UX 条款 | 分类 | 依据 |
|---|---|---|---|
| U-28 | §5.1 测评 UI 渲染仓库 `QuizQuestion`：目标词 + 仓库候选项 + 显式「Unsure / 不确定」+ `current/total` 进度 | `ALREADY_IMPLEMENTED_AND_VERIFIED`（口径见注） | `popup.ts:234` 标题 `测评中 X / 50`；`popup.ts:255-264` 渲染四个中文选项 + 独立「不确定」按钮（下标独立于四选项，对应 `unsureIndex`）；每日轮 `popup.ts:400` `进行中 X / Y`。**注**：首测当前为**整卷列表**（非逐题向导），进度口径为「已答 / 总数」；`QuizQuestion` 契约的四个要素（目标词 / 候选项 / 独立不确定 / 进度）**均已呈现**。若后续要求改为逐题向导式「当前第 N / total」，属交互形态扩展，须另走产品决策，不在 D-1～D-10 内 |
| U-29 | §5.1 移除二元 `Need Gloss / I Know This` 测评模型 | `SUPERSEDED_OLD_REQUIREMENT` | 仓库契约**从未包含**该二元模型（`QuizQuestion` 自始为四选一 + 不确定）。此条是外部 UX 草案的内部纠正；仓库侧动作仅为**确认不引入**，无代码影响 |
| U-30 | §5.1 首测与每日校准保持为两个独立领域流程，不得合并/重定义 | `ALREADY_IMPLEMENTED_AND_VERIFIED` | `InitialTestState` 与 `DailyTestState` 为两个独立契约与独立 message 路径；`RULES.md`「首测」/「每日校准轮」分节 |
| U-31 | §5.1 完成态给出明确完成确认 | `ALREADY_IMPLEMENTED_AND_VERIFIED` | `popup.ts:294` 「首测完成」+ 答对/答错/不确定统计 |
| U-32 | §5.2 估计只显示仓库估计 seam 返回值，禁止原型数字（`~5,420` / `5.4k`）与 CEFR 外推 | `ALREADY_IMPLEMENTED_AND_VERIFIED` | `popup.ts:360-362`：`你大概认识 N 个词` + `保守范围 low–high` + `基于当前 N 词覆盖估计，不做外推`；无 CEFR、无硬编码原型值 |

### 5.7 弹窗与设置（UX §6）

| ID | UX 条款 | 分类 | 依据 |
|---|---|---|---|
| U-33 | 固定紧凑布局 `320px` | `ALREADY_IMPLEMENTED_BUT_UX_DIFFERS` | 当前 `popup.css:26` 为 `380px` → 并入 D-8（D-8 现在的范围＝移除 Settings/本页后的 V0.1 popup 导航 + `320px` + 生词本搜索 + 中文空状态） |
| U-34 | 页签 1「This Page」：当前页面命中的词汇 | `OUT_OF_SCOPE`（**DEC-4 已裁定 `DEFER_THIS_PAGE_FROM_V0_1`**） | 当前无此能力。**延后为 V0.1 之后的候选**，不进本批 ticket；只延后 popup 的「本页」产品面，**不移除阅读面已需要的当前页瞬时处理**（见 §14） |
| U-35 | 页签 2「Notebook」：活跃学习词 + **搜索筛选** + 快捷状态切换 | `ALREADY_IMPLEMENTED_BUT_UX_DIFFERS` | 列表与「已掌握」快捷切换已实现；**无搜索筛选** → D-9 |
| U-36 | 页签 3「Level Check」：接入首测与每日校准 | `ALREADY_IMPLEMENTED_AND_VERIFIED` | 现有「测评」页签即承载首测 + 每日轮（页签名差异，非能力差异） |
| U-37 | 页签 4「Settings」 | `OUT_OF_SCOPE`（**DEC-2 已裁定 `DEFER_SETTINGS_FROM_V0_1`**） | V0.1 **不实现、不渲染** Settings 页签：不建空壳、不建禁用占位页签、不建推测性设置控件。未来 Settings 工作须另行取得产品授权（见 §14） |
| U-38 | `firstOccurrenceOnly` 从设置中移除、改为固定 V0.1 交互契约 | `ALREADY_IMPLEMENTED_AND_VERIFIED` | 仓库**从未有**该设置项：行内释义开关由策略计算（`occurrenceCount===1`），本就是固定契约 |
| U-39 | `showInlineMeaning` / 当前标签会话开关 / `activeDomainEnabled` / `pronounceOnHover` / `fontSize` 均为 `NOT AUTHORIZED` | `OUT_OF_SCOPE` | 仓库当前无任何设置持久化；本规格不授权引入 |
| U-40 | 移动端多级 bottom-sheet 行为 `NOT AUTHORIZED` | `OUT_OF_SCOPE` | 桌面 Chrome 扩展，非 V0.1 范围 |

### 5.8 错误与空状态（UX §7）

| ID | UX 条款 | 分类 | 依据 |
|---|---|---|---|
| U-41 | 未收录：零样式、零持久化、显式查询回 `当前词典未收录` | `ALREADY_IMPLEMENTED_AND_VERIFIED` | 见 U-16 |
| U-42 | 元数据失败：省略行内释义、浮层内回 `释义暂不可用` | `NEW_REQUIRED_CHANGE` | 见 U-18 → D-1 |
| U-43 | 空状态文案：生词本无匹配等 | `NEW_REQUIRED_CHANGE`（**DEC-3 已裁定 `CHINESE_FIRST`**） | 当前无对应空状态文案。**中文优先**：不使用 UX 原文的英文串（`No matching words.`），V0.1 给出简洁中文空状态；「本页无生词」因 DEC-4 延后「本页」页签而**不属本批** → D-10 |

### 5.9 外部 UX 自带的验证清单（UX §9）

`OUT_OF_SCOPE`（作为规范性要求）：该清单是 QA 检查表，不是产品需求。本规格将其可判定项吸收为 §11 的验收锚点，不单列实施条目。

---

## 6. CURRENT IMPLEMENTATION FACTS（2026-09-10 复核）

以下为**已存在且已验证**的行为。任何后续 ticket 若把它们写成「待实现」，视为规格缺陷。

1. **透明查询 span**：所有 query-eligible 词都包 `avr-word`；decision 只决定视觉类。known 词刷新后仍为 `className === 'avr-word'` 的可查询透明 span（E2E `e2e-verify.cjs:472-473`），**推翻** `RULES.md` 旧述「known 词被还原为纯文本且无交互载体」。
2. **稀疏灰线**：`hintDisplayDecision(effectiveFrequencyRank, status, threshold)` + bootstrap 阈值 `S[⌊n/2⌋]`；实测 light/100 词 ≈ 4.55。
3. **未收录响应**：`当前词典未收录`，click 不弹菜单、零持久化写入（E2E AC-10）。
4. **包外词**：可写 `WordState=learning`、可进入生词本并展示完整元数据、**不污染 `AssessmentEvidence`**（E2E `e2e-verify.cjs:1234-1241`，样本词 `serendipity`）。
5. **Tooltip 几何**：上方优先 / 下方翻转 / 不侵入 sticky / 不越视口 / 滚动后正确，均已 fresh 验证。
6. **行内释义首现契约**：`showInlineTranslation = isLearning && occurrenceCount === 1`，**不是用户设置**。
7. **真实鼠标拖选**：`mouseup` + `selectionchange` 真实时间线，E2E 覆盖真实 click 序列。
8. **无任何设置持久化**：`storage.ts` 无 settings 键；无 Settings 页签。
9. **无 `METADATA_RESOLUTION_FAILURE` 概念、无 `释义暂不可用` 文案**：全库零命中。
10. **Popup 为两页签**（测评 / 生词本），宽度 `380px`，无搜索筛选、无本页词表。
11. **点击层为极简操作菜单**（`会 / 不会` 两按钮，锚定于目标词左下），无词头/音标/词性/释义展示，无 Esc 关闭。

---

## 7. NEW REQUIRED DELTAS（当前唯一允许进入 backlog 的清单）

**决策状态**：`DEC-1`~`DEC-5` 已于 2026-09-10 全部 **CLOSED**（见 §10）。下表「决策状态」列为最终裁定；**没有任何 delta 仍被未决产品决策阻塞**。

| ID | Delta | 关联 UX | 决策状态 | 是否新增产品能力 |
|---|---|---|---|---|
| **D-1** | 引入 `METADATA_RESOLUTION_FAILURE` 领域术语与 `释义暂不可用` 兜底文案；明确其**不改写** `WordState` / `DisplayDecision`；轻提示/浮层在元数据缺失时显示该文案而非静默 | U-17 / U-18 / U-42 | —（无需决策） | 否（补既有不变式的可见表达） |
| **D-2** | 阅读面点击升级为 **Word Inspection Popover**：展示词头、`phonetic`、`pos`、`translation` + 「会 / 不会」；几何规则**复用 `calculateTooltipPosition` seam**（上方优先 / 不足下翻 / 视口 12px 夹取 / 不遮挡目标词 / 滚动同步），**禁止另写一套浮层几何** | U-9 / U-20 / U-24 | — | 否（替换现有极简菜单） |
| **D-3** | 浮层取消动作补齐 **Esc 关闭**（外部点击关闭已存在） | U-26 | — | 否 |
| **D-4** | `.avr-word` 显式声明 `font-family/size/weight/color/line-height/letter-spacing: inherit !important` | U-7 | — | 否 |
| **D-5** | 行内释义改为 `{posPrefix}{translation}` 格式；补 `user-select:none`、0.35em 间距、`line-height:1`、11–12px 斜体；**配色随 DEC-1 采用暖琥珀系，不得为红** | U-12 | **DEC-1 CLOSED → `AMBER_SYSTEM`** | 否 |
| **D-6** | 下划线配色统一为 DEC-1 琥珀族：light＝淡琥珀**点线**，learning＝淡琥珀**实线**；**不得出现红色或刺眼警示色**；light/learning 强度区分（点线 vs 实线）保持不变 | U-13 / U-19 | **DEC-1 CLOSED → `AMBER_SYSTEM`** | 否 |
| **D-7** | 拖选胶囊：**居中于选区上方**、**中文文案**（图标可选）；保留既有选区竞态处理与严格 token 校验，不得为对齐而重写机制 | U-14 / U-27 | **DEC-3 CLOSED → `CHINESE_FIRST`** | 否 |
| **D-8** | Popup V0.1 对齐：**不含**「本页」页签（DEC-4）**且不含**「Settings」页签（DEC-2）；保留并整理「生词本」「水平测评」导航，宽度 `320px` | U-33 / U-35 / U-36 / U-37 | **DEC-2 + DEC-4 CLOSED → 均延后** | **否**（原「本页」新增能力已移出） |
| **D-9** | 生词本搜索筛选 | U-35 | — | 是（最小新增交互） |
| **D-10** | 中文空状态文案（生词本无匹配等）；「本页无生词」因 DEC-4 **不属本批** | U-43 | **DEC-3 CLOSED → `CHINESE_FIRST`** | 否 |

**范围变化**：DEC-4 把 D-8 中原「本页」这一**唯一真正的新增产品能力**移出 V0.1。因此本批 delta **不含页面级新增产品能力**，全部为既有能力的形式 / 呈现补齐（唯一的新增交互是 D-9 生词本搜索筛选）。

**人工 dogfood 定位**：按 DEC-5，人工 dogfood 是**实施完成后的验收门**，不是实施的前置门（见 §14.3）。

---

## 8. 明确保留的外部 UX 权威纠正（不得被后续实现回退）

1. **测评契约**：首测/校准 UI 必须绑定仓库 `QuizQuestion`（目标词 + 仓库候选项 + 独立「不确定」+ `current/total`）。**不得恢复二元 `Need Gloss / I Know This` 模型。**
2. **流程不合并**：首测与每日校准是两个独立领域流程，不得合并为单一 UX/领域契约。
3. **元数据失败正交**：`METADATA_RESOLUTION_FAILURE` 与词汇学习状态正交；不得改 `WordState`、不得伪装成 known/learning/unknown、不得仅因元数据失败而改写词汇状态样式。
4. **词身份与状态权威**：沿用 `wordKey` 与显式状态优先级；**不得为迎合旧稿或旧 mockup 重新引入 `detailZh` / `nuance` / `ipa` / `zh` 字段。**

---

## 9. NON_GOALS

- 不重新设计 08-06 规格已批准的查询 / 提示 / 测评解耦架构；
- 不新增持久化 schema、不做存储迁移；
- 不引入任何设置项（`RULES.md` 未授权）；
- 不做 CEFR / 百分位 / 年级等外推展示；
- 不引入远程查词、云端画像、遥测或日志；
- 不恢复高置信自动隐藏、Pool B、PAV/Beta 后验、概率画像、复杂三桶配额等冻结项；
- 不把外部 UX 文档中的原型数字（`~5,420` / `5.4k` / `receptive vocabulary frontier`）变成生产值；
- 不因本规格放宽 `RULES.md` 的人工 dogfood 门（连续 7 天 / ≥20 篇 / 三项人工数字）与 `R-MIG-8` 真实 profile 备份要求。**DEC-5 澄清**：该门是**实施后**的验收门，不得被用作实施的前置门（见 §14.3）。

---

## 10. 产品决策（DEC-1~DEC-5，2026-09-10 全部 CLOSED）

| ID | 决策点 | 最终裁定 | 落点 | 影响 |
|---|---|---|---|---|
| **DEC-1** | 下划线配色 | **`AMBER_SYSTEM`**：V0.1 采用克制暖琥珀族——light＝淡琥珀点线，learning＝淡琥珀实线（首现另行内中文释义）；**不得仅因 learning 状态使用红色或刺眼警示色**。`#f59e0b` / `#d97706` 及其透明度为**非规范性**视觉参考，除非实现需要设计 token，否则不写成领域合同 | `RULES.md`「V0.1 呈现层与流程裁决」DEC-1；三条原「红色」规则已重标（原文存史）；08-06 规格 7 处已加 DEC-1 重标注 | D-5 / D-6 |
| **DEC-2** | Settings 页签 | **`DEFER_SETTINGS_FROM_V0_1`**：不实现、不渲染 Settings 页签；不建空壳页签、禁用占位页签或推测性设置控件；未来 Settings 工作须另行产品授权 | `RULES.md` DEC-2 + 「明确不做与冻结项」 | D-8（移除 Settings） |
| **DEC-3** | UI 文案语言 | **`CHINESE_FIRST`**：V0.1 用户可见文案用简洁中文（`会` / `不会` / `生词本` / `水平测评` / `不确定` / `释义暂不可用` / `当前词典未收录`）；**禁止 `Mark as Learning · 不会` 这类中英混排**，除非有真实技术或语言学理由且写明；内部领域标识符不变 | `RULES.md` DEC-3 | D-7 / D-10 |
| **DEC-4** | 「本页 / This Page」 | **`DEFER_THIS_PAGE_FROM_V0_1`**：延后为未来候选，不进本批 ticket；**只延后 popup 的「本页」产品面，不移除阅读面已需要的当前页瞬时处理** | `RULES.md` DEC-4 + 「明确不做与冻结项」；本规格 U-34 改判 `OUT_OF_SCOPE` | D-8（移除本页） |
| **DEC-5** | 实施与 dogfood 顺序 | **`IMPLEMENT_UX_DELTA_THEN_MANUAL_DOGFOOD`**：已验证自动化基线（RESUME-01 已建立）→ 实施已批准 UX delta → fresh 自动化门禁 → 真人 dogfood → `R-MIG-8` 真实 profile 备份/校验（适用时）→ V0.1 最终验收。**不得要求先对已知将被取代的 UI 做人工 dogfood** | `RULES.md` DEC-5；本规格 §14.3 | 全部 D-* |

**结论**：无任何未决产品决策阻塞确定性 ticket 编写。

---

## 11. 验收锚点（实施阶段才执行）

- `npm run typecheck` / `npm test` / Python data tests / `npm run build` 保持全绿（基线：283 + 12）；
- `AVR_E2E_NO_SANDBOX=1 npm run test:e2e` 保持 `E2E ALL PASS`（Chrome for Testing 153；环境前置条件见 README）；
- 新增/变更断言必须落在：D-1 兜底文案与零状态写入、D-2 浮层几何（上下翻转 / 12px 夹取 / 滚动同步 / 不遮挡目标词）、D-3 Esc 关闭、D-4 宿主 `span{}` 污染抗性、D-5 释义格式与 `user-select:none`、D-7 胶囊位置、D-9 搜索筛选、D-10 空状态；
- 负向断言：未收录词零持久化（既有 AC-10 不得回归）、包外词不污染 `AssessmentEvidence`（既有 T-NB-6 断言不得回归）、渲染提示/悬停不创建或改写 `WordState`；
- 隐私断言：快照不含 URL / 标题 / 正文 / 句子 / 域名。

---

## 12. 实施 ticket 资格

**当前：`READY_FOR_TICKET_DECOMPOSITION`。**

依据：

1. **权威快照自包含**——`PHASE-DEC-TICKETS` §1 已修复并校验：本分支 HEAD 可读 `docs/specs/2026-09-04-UX_SPEC_V1.2.1.md`，blob 与 `origin/main` 一致，冻结状态字串未变；
2. **决策闭合**——`DEC-1`~`DEC-5` 全部 CLOSED，落点为 `RULES.md`「V0.1 呈现层与流程裁决」，与集成规格无矛盾；
3. **事实与 delta 边界明确**——§6「已验证实现事实」与 §7「新增 delta」无重叠，每张 ticket 都能区分「需保留」与「需新增」；
4. **无未决产品决策**——§10 为空阻塞；
5. **延后项已显式移出**——「本页」与「Settings」见 §14，不进本批。

本批 ticket 位于 `work/tickets/2026-09-10-v0.1-ux-delta/`（README 含依赖 DAG 与执行顺序）。ticket 只描述**已验证行为需保留**与**新增 delta**；**ticket 本身不授权开始开发**，开发仍须用户另行明确授权。

---

## 13. 取代与历史化声明

- 本规格**不取代** `2026-08-06-V0.1-查询交互提示与测评词包解耦规格.md` 的产品方向，只在其之上补充 UX 呈现层合同；两者冲突时以 `RULES.md` 为准。
- 本规格**不取代** `2026-08-05-V0.1-阅读体验增强规格.md`；其 tooltip 定位、拖选入口等目标已被 main 实现，相关「实现差距」叙述由本次治理一并历史化。
- 外部 `UX_SPEC_V1.2.1` 通过本文件进入仓库权威；**其原文不得被直接当作实施依据**（它是输入，不是授权）。
- 外部 UX 文档 §9 的验证清单不是产品需求，仅作 §11 的输入。

---

## 14. 延后候选与最终验收门

### 14.1 延后：本页 / This Page（DEC-4 = `DEFER_THIS_PAGE_FROM_V0_1`）

| 项 | 内容 |
|---|---|
| 状态 | **V0.1 延后候选**，不进本批实施 ticket |
| 能力范围 | popup 中展示「当前页面命中的词汇」列表（页面级词表产品面） |
| 延后理由 | 它是 D-1~D-10 中**唯一真正的新增产品能力**；完成冻结 UX 的阅读 / 查询 / 生词本 / 测评交互对齐并不需要它 |
| **不延后的部分** | 阅读面已需要的**当前页瞬时处理**（扫描、`wordKey` 解析、标注、选区、tooltip / 浮层）**保持不变**；任何 ticket 不得以「本页已延后」为由移除或削弱它 |
| 重启条件 | V0.1 最终验收后由产品重新授权，届时需新 Spec 与独立 ticket 批次 |

### 14.2 延后：Settings（DEC-2 = `DEFER_SETTINGS_FROM_V0_1`）

| 项 | 内容 |
|---|---|
| 状态 | **V0.1 明确不做** |
| 禁止范围 | Settings 页签、任何设置持久化 schema、空壳页签、禁用占位页签、推测性设置控件 |
| 延后理由 | UX 自身把该页签下**所有**设置项标为 `OPEN / NOT AUTHORIZED`（U-39），页签无内容可呈现 |
| 当前事实 | 仓库当前无任何设置持久化，`storage.ts` 无 settings 键，无 Settings 页签 |
| 重启条件 | 单独的产品授权 + 新 Spec / ticket |

### 14.3 最终验收门：`V0.1 FINAL DOGFOOD`（本阶段**只表示、不执行**）

按 DEC-5，本门位于**全部已批准 UX 实施切片通过 fresh 自动化门禁之后**：

1. 真实 Chrome **真人 dogfood**（连续 7 天 / ≥20 篇固定门槛不变，三项人工数字：不必要提示 / 释义不可用 / 覆盖缺失）；
2. `R-MIG-8` 真实 profile **备份 / 校验**（适用时）；
3. **隐私与存储检查**：快照不含 URL / 标题 / 正文 / 句子 / 域名；
4. **阅读 UX 检查**：琥珀视觉族、行内释义首现契约、Inspection Popover、Esc 关闭、拖选胶囊；
5. **测评流程**：首测 / 每日校准 / 估计展示；
6. **生词本**：搜索筛选、状态切换、空状态；
7. **元数据失败行为**：显示 `释义暂不可用`，**零 `WordState` 写入**；
8. **宿主页面干扰检查**：排版继承、零布局位移、滚动与选择行为；
9. **边界**：ECDICT 中文释义公开再分发权利链是**独立的发布阻断项**，不得与本地 V0.1 dogfood readiness 混淆或互换。

### 14.4 工程工具候选（非产品）

`SPIKE-CHROME-DEVPROFILE`（Chrome 开发/测试 profile 与 DevTools MCP 工作流评估）为**工具链候选**，见 `work/tickets/2026-09-10-v0.1-ux-delta/SPIKE-CHROME-DEVPROFILE.md`。它**不改变任何产品语义**，且**不阻塞**本批实施 ticket。
