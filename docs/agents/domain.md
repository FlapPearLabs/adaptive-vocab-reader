# 领域文档

本项目采用 single-context。

## 开始探索前

- 阅读根目录 `CONTEXT.md`。
- 阅读与当前工作相关的 `docs/adr/`。
- 文件不存在时安静继续，不得为了完整目录而创建空文件。

## 领域语言

Issue、测试、架构方案和代码命名应使用 `CONTEXT.md` 中定义的规范术语，避免使用其中明确排除的同义词。

发现领域概念缺失时，通过 `/domain-modeling` 澄清；不得在实现中自行发明含义。

## ADR 冲突

若方案与现有 ADR 冲突，必须明确指出冲突并说明为何需要重新讨论，不得静默覆盖。
