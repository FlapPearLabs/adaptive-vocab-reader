# Handoff — V0.1 浏览器词汇插件（adaptive-vocab-reader）— 进入 Ticket 05

> 生成时间：2026-08-03。目的：让全新代理接续实现 Ticket 05（真浏览器综合验收门）。
> 关联新任务提示词：`work/handoff/2026-08-03-ticket05-prompt.md`（本文件即该提示词「输入」首项所引用的背景归纳文档），二者配套使用。
> 不重复 specs/plans/ADRs/commits 内容，按路径引用。

## 现状（2026-08-03）

- 远端 main = `0f402fc`；本地 main 与之同步，工作区干净。
- 远端：`https://github.com/FlapPearLabs/adaptive-vocab-reader.git`（origin）。
- 已实现并归档的 ticket（01~04 全部经网页版 GPT 审查 PASS 后合并 main）：
  - **01 切断 V0.1 审计用户路径**（R-AUD-1~5）：合并 commit `0303836`。
  - **02 wordKey / AssessmentEvidence / schema 3**（R-KEY-1~4、R-EVD-1~4、R-MIG-1~8）：合并 commit `6f5272b`。schema 3 的 `dailyTest: null` 与 `completedRoundIndex: 0` 已正式初始化。
  - **03 首测词汇量估计展示**（R-EST-1~7）：实现 `e8339f9` + 审查修复 `b15ddf3`，快进合并 main。落地 `strategy/estimate.ts` 纯函数 seam（点值 + 双侧 90% Wilson 保守范围 + unavailable + 词包参数化）。
  - **04 每日五题校准轮**（R-EVD-5、R-DLY-1~9）：实现 `0146d5b` + CODE 审查 5 BLOCKER 修复 `0f402fc`，二次审查 PASS 后快进合并 main。落地 `strategy/daily.ts`（奇偶频段选题/结算、date seam=localDate）、`DailyTestState` 正式类型、worker 跨日写入边界（today date seam）、popup 每日入口/跳过/暂停/跨日过期/次级反悔。
- 待办 ticket（2 个）：**05 真浏览器综合验收门**（本票）、**06 人工 dogfood 门**。
- 真实验证基线（01~04 均全绿）：`npm run typecheck` / `npm test`（**267 单测**）/ `npm run build` / `npm run test:e2e`（真实 Chrome；本机受限会话需 `AVR_E2E_NO_SANDBOX=1`，否则 Chrome 151 `Requesting main frame too early` 竞态，与代码无关）。

## 权威来源（必读，按优先级；冲突时以此顺序裁决）

> **本 handoff 仅作导航摘要，不是需求来源。** 任何与权威文件冲突处，一律按下列顺序裁决，不得用本归纳文档覆盖 `RULES.md` / `CONTEXT.md` / Spec / ADR 的领域术语或施工规格。

1. `RULES.md` — [已确认] vs [待确认] 唯一产品规则来源。本次重点「交付、安全与下一步」（V0.1 最高层验收 seam 为真浏览器 E2E）与「存储与迁移（schema 3）」（R-MIG-8 备份门时机）。
2. `CONTEXT.md` — 领域术语唯一权威。
3. `docs/specs/2026-07-30-V0.1-重新对齐规格.md` — 当前施工规格。本次重点 **§21 真浏览器 E2E（场景 1~17 + 场景 14/15 约束 + 持久化验收补全）**、§23 反过度设计门禁、§24 风险与延期项。
4. `docs/adr/0004-词汇键与测试证据分离.md` — wordKey/证据分离 ADR（双真相源；每日作答双写）。
5. `work/tickets/2026-07-31-v0.1-realign/05-real-browser-acceptance-gate.md` — 本票规格。
6. `AGENTS.md` — 项目代理工作流（Skill 路由、推送授权、质量门禁）；工作流文件，非需求来源。
- 旧规格（2026-07-22 三份）、旧 GitHub Issue #1~#5 仅作历史证据，冲突时让位，不得作为新增需求依据；不得臆造需求。

## 证据（已完成项的真实验证，T5 复核的既有覆盖）

- `e2e-verify.cjs` 现状（main 上）已覆盖：
  - 阶段一（#1）：真实构建产物加载 + 静态正文/SPA 阅读标注 + 手动状态持久化 + 词形共享 wordKey（abilities→ability）+ 快照最小隐私。
  - 阶段一 B（#1B）：**真实 schema 2 fixture 经真实 worker/storage 启动路径升级 v3**（R-MIG-7：重启读 storage → loadSnapshot → migrateSnapshot 写回 → 二次重启恒等）。场景 14 的主体已存在。
  - 阶段二（#2）：50 题首测 + 冻结计划 + 多标签页同步（**同一 word 跨两页**）+ 估计展示（场景 5）+ manual 不改证据/估计（场景 6）+ 审计入口不可达（场景 16）+ 残留 auditPlan 不恢复（R-AUD-2）+ **同 profile 重启后重开 popup 恢复已完成摘要**（场景 15 的主体）。
  - 阶段三（#3）：SPA 动态插入/路由切换/性能观测。
  - 阶段四（#4）：每日校准轮场景 7~13、17（含未完成轮跨日、跨日写入拒绝、进行中阅读、跳过/暂停/反悔）。
- 单测 267（strategy/worker/content/shared，含 audit 冻结旧测试原样保留未扩建）。

## 下一步：Ticket 05（真浏览器综合验收门）

- 文件：`work/tickets/2026-07-31-v0.1-realign/05-real-browser-acceptance-gate.md`（Status: ready-for-agent；Blocked by 01~04 已全部解除）。
- 主责任：**不持有任何 R-\* 主责任**。批准 Spec §21 的综合真实浏览器验收，复核 T1~T4 已有 Requirement（主责任归属保持不变）。
- 目标：在真实交付路径上一次性锁定完整闭环，并给出「可进入人工 dogfood / 不可进入」的明确结论。
- 范围（严格限于允许的六项）：
  1. 真实 Chrome 扩展交付路径（加载 build 产物）；
  2. 真实 schema 2 fixture 经真实 worker/storage 路径升级 v3（场景 14；**不得只调迁移纯函数冒充**）；
  3. 浏览器重启后持久化（**同时验证** `WordState`、`AssessmentEvidence`、`DailyTestState`、`completedRoundIndex`、`schemaVersion=3` 五项，场景 15）；
  4. 两个真实标签页含**同一 wordKey 不同词形**（如 ability/abilities）时 manual / daily 更新后**同步**（Spec §21 持久化验收补全；现有 #2 仅验证同一 surface 跨两页，不同词形共享 wordKey 的跨标签同步需 T5 显式覆盖）；
  5. Spec §5 完整用户闭环回归（场景 1~17 综合回归，任一场景失败 → 「不放行」并定位）；
  6. dogfood 前放行结论。
- 非目标（重要，反过度设计）：不承担 T1~T4 的单测/迁移单测/局部 E2E；不新增产品功能；不为冻结 audit 补测试（R-AUD-5）；不建 CI 平台/报告 dashboard/遥测；不重新取得 T1~T4 的 R-\* 主责任。
- 测试 seam：只升级现有 `e2e-verify.cjs`（不引入测试框架迁移）；`tests/fixtures/` 新增真实 schema 2 快照 fixture；`build.mjs`/dist 真实构建产物；`package.json` 的 `test:e2e` 脚本。
- 验收示例（取自 ticket）：注入真实 schema 2 快照后经扩展自身 worker/storage 升级 v3（非纯函数冒充）；浏览器重启后五项持久化断言全通过；两标签页同 wordKey 不同词形同步；闭环回归无回归项；输出明确放行/不放行结论。
- 复核矩阵：ticket 内 §21 场景 → 来源 Ticket → 主责任 R-ID 一一对应（场景 1~17），T5 不得改变主责任归属。

## 未决项 / 风险

- **R-MIG-8 备份门时机（关键）**：真实用户 profile 首次加载 schema 3 构建前，必须保留原 schema 2 chrome.storage 快照副本并验证可解析；实际执行时机为 **T5 隔离 E2E 全绿之后、T6 人工 dogfood 开始之前**，且须用户明确授权与配合。**T5 的隔离 E2E 不以真实用户备份已经执行为前置条件**；T5 不得触碰真实 Chrome profile（仅隔离 fixture/临时 profile）。
- T5 综合回归若发现 T1~T4 主责任的缺陷：**T5 当前任务只负责报告失败**——输出「不可进入 dogfood」结论，定位到失败场景、R-ID 与责任 Ticket，并附复现证据；**缺陷修复必须离开 T5 范围**，交由对应主责任范围的后续任务另行处理（「走对应主责任」意味着不是由 T5 当前分支代修，T5 不持有任何 R-* 主责任），修复合并 main 后再重新执行 T5 综合验收。
- 双标签页不同词形同步的测试数据：固定 1,000 词包没有 go/went，用产物中实际存在的单复数对（如 ability/abilities，T2 #1 已用同一对验证词形合并）构造跨标签同步验证。
- 本机 E2E 需 `AVR_E2E_NO_SANDBOX=1`（受限会话 Chrome 151 竞态，与代码无关）。

## 安全边界（不得越界）

- 不修改/删除/加固冻结审计模块（`strategy/audit.ts`、`worker/auditValidation.ts`、`shared/auditPlanVersion.ts`、worker 审计 handler 与既有旧审计测试）；不为其补测试（R-AUD-5）。
- 不自行扩大范围或执行外部写入（不创建 GitHub Issue/PR、不部署、不改生产配置）除非用户显式授权。
- 不碰真实 Chrome profile（仅隔离 fixture/临时 profile）；不执行 R-MIG-8 真实备份（属 T6 前人工门，须用户授权）。
- 推送前先 `unset GH_TOKEN`；快进合并 main；不强合并、不 force、不关闭 Issue。
- commit/push 需用户显式肯定确认（「可以/做吧/同意/OK/yes」）才授权；沉默或「先别动」不算同意。

## 建议技能（suggested skills）

- 进入 05 实现：显式调用 `/implement`（仅 Spec/验收明确后）+ `/tdd`（红—绿—重构；E2E 断言命中真实交付路径，不得只测 mock）。
- 交互/算法可行性：如需原型用 `prototype`（可丢弃）。
- 完成实现后：`code-review`（声明固定比较基点，本次 `0f402fc`）。
- 网页版 GPT 文档/Spec 审查提示词模板：`outputs/2026-08-01-gpt-doc-review-prompt.md`（方式 A 推 review 分支 + Compare 链接；方式 B 粘贴全文）。
- 协作路由：本地 review 分支 → 网页版 GPT 审查（PASS/CHANGES_REQUESTED，输出 VERDICT/BLOCKERS/NON_BLOCKING/TASK_ROUTING/NEXT_AGENT_PROMPT）→ Codex 交接包 → 合并 main。详见 `AGENTS.md` §5 任务路由矩阵与 Git 安全规则，以及 `.workbuddy/memory/MEMORY.md` 协作规则。

## 交接输入清单（下一会话直接可用）

- main tip：`0f402fc`（T1~T4 全部合并；单测 267；E2E 场景 1~17 主体已存在）
- Ticket 05 路径：`work/tickets/2026-07-31-v0.1-realign/05-real-browser-acceptance-gate.md`
- 规格：§21（场景 1~17 + 场景 14/15 约束 + 持久化验收补全）、§23、§24；§5 完整用户闭环
- 既有 E2E 覆盖对照：场景 14 主体在 #1B（schema2→3 真实路径）、场景 15 主体在 #2（重启恢复）+ #4（跨日重启）、场景 7~13/17 在 #4；**双标签页同 wordKey 不同词形同步、重启后五项持久化断言并查、全场景综合回归与放行结论**为 T5 新增重点
- 已落地数据模型：`VocabSnapshot`（schema 3：`words` / `assessmentEvidence` / `initialTest` / `dailyTest` / `completedRoundIndex`）、`AssessmentEvidence{outcome,source,assessedAt}` 每词最新一条、`DailyTestState{localDate,roundIndex,questions,answers,completed,skipped}`
- R-MIG-8：T5 隔离 E2E 全绿后、T6 开始前，由用户明确授权与配合执行真实 profile 备份
